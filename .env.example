# Database
DATABASE_URL="file:./data/docktor.db"

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Docktor paths
DOCKTOR_BASE_URL=http://localhost:8443
DOCKTOR_STACKS_DIR=/stacks
DOCKTOR_DATA_DIR=/data
DOCKTOR_BACKUP_DIR=/backups

# Client dist path (production only)
# CLIENT_DIST_PATH=./client-dist
