---
phase: 02-observability
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/jobs/file-watcher.ts
  - server/src/lib/compose-parser.ts
autonomous: true
requirements: [FW-01, FW-02]
gap_closure: true

must_haves:
  truths:
    - "Modified docker-compose.yml service metadata (image version, ports, volumes) appears in UI and database"
    - "Invalid YAML syntax or missing services key triggers config_error SSE event"
    - "Config error messages visible in server logs"
  artifacts:
    - path: "server/src/jobs/file-watcher.ts"
      provides: "Calls replaceServices to sync database with compose file changes"
      min_lines: 200
    - path: "server/src/lib/compose-parser.ts"
      provides: "Throws errors for invalid compose structure"
      min_lines: 70
  key_links:
    - from: "server/src/jobs/file-watcher.ts"
      to: "createComposeConfig"
      via: "import and call after parse validation"
      pattern: "createComposeConfig\\(content\\)"
    - from: "server/src/jobs/file-watcher.ts"
      to: "replaceServices"
      via: "repo method call"
      pattern: "replaceServices\\(.*composeConfig"
    - from: "server/src/lib/compose-parser.ts"
      to: "throw Error"
      via: "validation checks"
      pattern: "throw new Error"
---

<objective>
Fix FileWatcher to sync service metadata with compose file changes and detect configuration errors.

Purpose: Close UAT gaps where config changes (version, ports) are detected but not reflected in database/UI (Test 4), and invalid YAML errors are not shown (Test 5).

Output: FileWatcher updates service metadata in database when compose files change; parser throws descriptive errors for invalid compose structure.
</objective>

<execution_context>
@C:/Users/D070307/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/D070307/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-observability/02-UAT.md
@.planning/debug/config-data-not-updating.md
@.planning/debug/config-error-not-shown.md
@.planning/phases/02-observability/02-03-SUMMARY.md

<interfaces>
<!-- Key types FileWatcher must use -->

From server/src/domain/compose-config.ts:
```typescript
export interface ComposeConfig {
    readonly hash: string;
    readonly services: readonly ParsedService[];
}

export function createComposeConfig(content: string): ComposeConfig {
    return {
        hash: hashComposeContent(content),
        services: parseComposeContent(content),
    };
}
```

From server/src/repositories/stack-repository.ts:
```typescript
async replaceServices(stackId: string, composeConfig: ComposeConfig): Promise<void>
```

From server/src/lib/compose-parser.ts:
```typescript
export interface ParsedService {
    serviceName: string;
    image: string;
    imageTag: string | null;
    ports: {host: number; container: number; protocol: string}[];
    volumes: {hostPath: string; containerPath: string}[];
}

export function parseComposeContent(content: string): ParsedService[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parser throws errors for invalid compose structure</name>
  <files>server/src/lib/compose-parser.ts</files>
  <action>
Modify parseComposeContent() to throw descriptive errors instead of silently returning empty array when compose structure is invalid.

Add validation checks after parseYaml(content) on line 50:
1. If `doc?.services` is missing, null, undefined, or not an object → throw Error("Compose file missing 'services' key")
2. If services object is empty (no service definitions) → throw Error("Compose file has empty services section")
3. Keep existing behavior for individual service validation (filter/map pattern lines 56-63)

Do NOT add network/volume directory restrictions - those are user expectations without requirements.

Expected behavior after fix:
- Invalid YAML syntax: parseYaml throws (already works)
- Missing services key: parseComposeContent throws new error
- Empty services: parseComposeContent throws new error
- Valid compose: returns ParsedService[] array (unchanged)
  </action>
  <verify>
    <automated>
# Test invalid scenarios throw errors:
node -e "
const {parseComposeContent} = require('./server/src/lib/compose-parser.js');
try { parseComposeContent('version: \"3\"'); console.error('FAIL: should throw'); process.exit(1); } catch(e) { if(!e.message.includes('services')) { console.error('FAIL: wrong message'); process.exit(1); } }
try { parseComposeContent('services: {}'); console.error('FAIL: should throw'); process.exit(1); } catch(e) { if(!e.message.includes('empty')) { console.error('FAIL: wrong message'); process.exit(1); } }
console.log('PASS: parser throws correctly');
"
    </automated>
  </verify>
  <done>parseComposeContent throws descriptive errors for missing/empty services; existing valid compose parsing unchanged</done>
</task>

<task type="auto">
  <name>Task 2: FileWatcher updates service metadata on config changes</name>
  <files>server/src/jobs/file-watcher.ts</files>
  <action>
Extend FileWatcher.handleFileChange() to sync service metadata with database after validating compose file changes.

Current code flow (lines 141-157):
1. Parses compose for validation (line 142) but discards result
2. Catches errors and broadcasts config_error (lines 143-157)
3. Updates hash and broadcasts config_changed (lines 160-171)

New flow after line 157 (after catch block, before line 159 comment):
1. Import createComposeConfig from "../domain/compose-config.js"
2. Create ComposeConfig: `const composeConfig = createComposeConfig(content)`
3. Extend FileWatcherRepo interface to include `replaceServices(stackId: string, composeConfig: any): Promise<void>`
4. Call repo.replaceServices: `await repo.replaceServices(stack.id, composeConfig)` before updateStackHash
5. Add console.log: `[FileWatcher] Updated service metadata for ${stack.id} (${composeConfig.services.length} services)`

Reference implementation pattern from stack-service.ts lines 58-59 (see context above).

Sequence matters: replaceServices BEFORE updateStackHash so if replaceServices fails, hash stays old and reconcile will retry.

Error handling: replaceServices errors should propagate up to handleFileChange caller (lines 72, 79) where they're logged - no try/catch needed here.
  </action>
  <verify>
    <automated>npm run build -w server</automated>
  </verify>
  <done>FileWatcher calls createComposeConfig and replaceServices on every config change; TypeScript compiles; replaceServices added to FileWatcherRepo interface</done>
</task>

</tasks>

<verification>
1. Build succeeds: `npm run build -w server` exits 0
2. Parser validation: missing/empty services throw descriptive errors
3. FileWatcher imports createComposeConfig from correct path
4. FileWatcher calls replaceServices before updateStackHash
5. replaceServices added to FileWatcherRepo interface with correct signature
</verification>

<success_criteria>
- parseComposeContent() throws "missing 'services' key" for compose files without services
- parseComposeContent() throws "empty services section" for compose with `services: {}`
- FileWatcher.handleFileChange() calls createComposeConfig after validation
- FileWatcher.handleFileChange() calls repo.replaceServices before updating hash
- TypeScript compilation passes
- Gap 1 (Test 4) closed: config metadata updates sync to database
- Gap 2 (Test 5) closed: invalid config triggers config_error events
</success_criteria>

<output>
After completion, create `.planning/phases/02-observability/02-06-SUMMARY.md`
</output>
