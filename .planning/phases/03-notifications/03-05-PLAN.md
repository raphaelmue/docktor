---
phase: 03-notifications
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/jobs/notification-watcher.ts
  - server/src/routes/settings.ts
  - server/src/jobs/disk-checker.ts
  - server/src/lib/state-broadcaster.ts
  - server/src/application/notification-service.ts
  - server/src/application/index.ts
  - client/src/routes/app/settings.tsx
  - client/src/hooks/use-container-events.ts
  - client/src/hooks/use-stack.ts
  - server/test/unit/jobs/disk-checker.test.ts
  - .env.example
autonomous: false
gap_closure: true
requirements: [NOTF-01, NOTF-03, NOTF-04, NOTF-06]

must_haves:
  truths:
    - "SMTP password and from address are stored when saving SMTP settings"
    - "DiskChecker runs without crashing on Windows"
    - "Disk warning threshold inputs are visible and functional"
    - "Notification log auto-refreshes when new notifications are created"
    - "NotificationWatcher logs startup message confirming subscription"
  artifacts:
    - path: "server/src/routes/settings.ts"
      provides: "SMTP from field saved before password encryption block"
      contains: "smtp.from.*before.*smtp.password"
    - path: "server/src/jobs/disk-checker.ts"
      provides: "Platform-specific disk monitoring path"
      contains: "DOCKER_DATA_PATH"
    - path: "client/src/routes/app/settings.tsx"
      provides: "Disk threshold input fields"
      contains: "diskThresholdPercent"
    - path: "server/src/lib/state-broadcaster.ts"
      provides: "NotificationCreatedEvent type"
      contains: "notification_created"
  key_links:
    - from: "server/src/application/notification-service.ts"
      to: "server/src/lib/state-broadcaster.ts"
      via: "broadcaster.publish notification_created"
      pattern: "broadcaster\\.publish.*notification_created"
    - from: "client/src/routes/app/settings.tsx"
      to: "client/src/hooks/use-container-events.ts"
      via: "useContainerEvents subscription"
      pattern: "useContainerEvents.*notification_created"
---

<objective>
Close UAT gaps from Phase 03 verification: fix SMTP storage, Windows disk checker, missing threshold inputs, notification log SSE, and add startup logging.

Purpose: Address 7 UAT issues (Tests 3, 4, 5, 11, 12, 16, 18) reported during user acceptance testing. Most fixes are already implemented in working directory but uncommitted.
Output: All notification features work as specified in UAT, committed and ready for re-verification.
</objective>

<execution_context>
@C:/Users/D070307/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/D070307/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-notifications/03-UAT.md
@.planning/debug/smtp-password-not-stored.md
@.planning/debug/diskchecker-enoent-windows.md
@.planning/debug/disk-warning-inputs-missing.md
@.planning/debug/notification-log-no-auto-refresh.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NotificationWatcher startup logging and commit all gap fixes</name>
  <files>server/src/jobs/notification-watcher.ts</files>
  <action>
Add startup logging to NotificationWatcher.start() method to confirm subscription to StateBroadcaster.

In `server/src/jobs/notification-watcher.ts`:
1. Add `console.log("[NotificationWatcher] Started - subscribed to StateBroadcaster")` at the end of the `start()` method (after `this.unsubscribe = this.broadcaster.subscribe(...)`)
2. Add `console.log("[NotificationWatcher] Stopped")` at the start of the `stop()` method

This addresses UAT Test 12: "NotificationWatcher I didnt see any logs."

Then commit ALL uncommitted gap closure changes from the debug sessions:
- SMTP from field moved before password encryption (server/src/routes/settings.ts)
- DiskChecker Windows DOCKER_DATA_PATH env var (server/src/jobs/disk-checker.ts, server/test/unit/jobs/disk-checker.test.ts)
- Disk warning threshold inputs (client/src/routes/app/settings.tsx)
- Notification log SSE refresh (server/src/lib/state-broadcaster.ts, server/src/application/notification-service.ts, server/src/application/index.ts, client/src/hooks/use-container-events.ts, client/src/hooks/use-stack.ts)
- .env.example with ENCRYPTION_KEY and DOCKER_DATA_PATH

Commit message: "fix(03-notifications): close UAT gaps for SMTP storage, disk checker, threshold inputs, and SSE refresh"
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit && yarn test 2>&1 | head -50</automated>
  </verify>
  <done>NotificationWatcher logs startup message, all gap fixes committed in single atomic commit</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all UAT gap closures work correctly</name>
  <files>N/A</files>
  <action>
Human verification of all gap closures.

**What was built:**
All UAT gap closures for Phase 03 notifications:
1. SMTP password + from address storage (Tests 3, 4, 5)
2. DiskChecker Windows support via DOCKER_DATA_PATH (Tests 11, 12)
3. Disk warning threshold input fields (Test 16)
4. Notification log auto-refresh via SSE (Test 18)
5. NotificationWatcher startup logging (Test 12)

**How to verify:**

**Pre-requisite:** Ensure ENCRYPTION_KEY is set in your .env file. Generate one with:
```
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

**Test 1: SMTP Storage (Tests 3, 4, 5)**
1. Navigate to Settings > Notifications tab
2. Fill in SMTP config: host, port, user, password, from email, from name
3. Click Save
4. Refresh the page
5. Verify all fields except password are populated (password shows placeholder)
6. Click Test Connection - should succeed or show meaningful error

**Test 2: DiskChecker Windows (Tests 11, 12)**
1. Start the server with `yarn workspace @docktor/server dev`
2. Check console logs - should NOT see "[DiskChecker] statfs failed" error
3. On Windows, it now monitors "." (current directory) without ENOENT error
4. Check for "[NotificationWatcher] Started - subscribed to StateBroadcaster" log message

**Test 3: Disk Threshold Inputs (Test 16)**
1. Navigate to Settings > Notifications tab
2. Enable "Disk Warnings" toggle
3. Verify two input fields appear: Threshold (%) and Threshold (bytes)
4. Change percent to 15, blur field - should show "Threshold updated" toast
5. Change bytes to "5 GB", blur field - should show "Threshold updated" toast
6. Refresh page - values should persist

**Test 4: Notification Log Auto-Refresh (Test 18)**
1. Open Settings > Notifications tab in one browser tab
2. In another tab/window, trigger an action that creates a notification (e.g., deploy a stack that goes to ERROR)
3. Without refreshing the first tab, observe if new notification appears in the log automatically

**Resume signal:** Type "approved" or describe which tests failed
  </action>
  <verify>User confirms all 4 test scenarios pass</verify>
  <done>All UAT gaps verified working by human tester</done>
</task>

</tasks>

<verification>
Run TypeScript type checking and unit tests:
```bash
cd server && npx tsc --noEmit && yarn test
cd ../client && npx tsc --noEmit
```

All tests pass, zero TypeScript errors.
</verification>

<success_criteria>
1. NotificationWatcher logs startup message confirming StateBroadcaster subscription
2. All 7 UAT gaps addressed and committed
3. User verifies all 4 test scenarios pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-notifications/03-05-SUMMARY.md`
</output>
