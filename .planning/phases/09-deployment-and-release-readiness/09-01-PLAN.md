---
phase: 09-deployment-and-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - .env.production
  - docs/deployment.md
  - .planning/todos/completed/2026-08-27-document-deployment-config-clean-env-and-docker-compose.md
autonomous: true
requirements:
  - "n/a — phase scoped by four todo files, not by REQUIREMENTS.md IDs. This plan closes: .planning/todos/pending/2026-08-27-document-deployment-config-clean-env-and-docker-compose.md (scope item 1, decision D-01)"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-01: an operator following `.env.example`'s own header comment ends up with a file named exactly `.env` at the project root — the same filename `docker-compose.yml`'s `env_file:` entry loads and the same filename `docs/deployment.md` instructs — so Compose's top-level `${VAR}` interpolation can see DOCKTOR_STACKS_HOST_DIR."
    - "D-01: every factual claim `docs/deployment.md` makes about `docker-compose.yml`, `.env.example`, and `Dockerfile` (variable names, defaults, requiredness, volume/mount layout, published ports) is confirmed true against those three files as they exist today, with each checked fact and its verdict recorded in the SUMMARY."
    - "D-01: the deployment-config todo is filed under .planning/todos/completed/ with a resolution that names which prior phase shipped each defect the todo originally listed, and names the one drift this plan itself fixed."
    - "If the execution environment blocks access to `.env.production`, that blockage is reported as a named remaining gap in the SUMMARY with the exact edit a developer must apply — it is never silently skipped or silently claimed done."
  prohibitions:
    - "D-01 forbids new implementation work for scope item 1 — no new deployment feature, no new env var, no docker-compose.yml or Dockerfile behavior change may be introduced by this plan."
    - "This plan must not edit the `## Database schema` section of docs/deployment.md — that section is accurate for today's shipped behavior and is rewritten by plan 09-04 after the Prisma migrate cutover lands."
  artifacts:
    - ".env.example — header comment names the project-root env filename Compose actually loads"
    - ".env.production — same header correction, or a SUMMARY-recorded access blockage with the verbatim edit"
    - "docs/deployment.md — only if the audit finds a divergence outside the Database schema section"
    - ".planning/todos/completed/2026-08-27-document-deployment-config-clean-env-and-docker-compose.md — moved and stamped"
  key_links:
    - "docker-compose.yml `env_file: - .env` -> Compose's top-level ${VAR} interpolation -> DOCKTOR_STACKS_HOST_DIR volume pairing. The template's header comment is the only place an operator learns which filename makes this chain work; a wrong name there silently breaks the stacks-directory mount (the exact 05.1-10 defect)."
    - "`gsd_run query todo complete <filename>` is the single mechanism that both moves a todo and stamps its completed/status frontmatter — a hand-rolled git mv moves the file without the stamp."
---

<objective>
Close scope item 1 (deployment config documentation) by fixing the one real drift research found and proving the rest of the deployment guide is still true.

D-01 assumed item 1 was a zero-work close. 09-RESEARCH.md disproved that with a file:line citation: `.env.example` line 3 still tells operators to copy the template to the pre-05.1-10 filename, while `docker-compose.yml` line 23 and `docs/deployment.md` line 41 both agree on the current one. That is the same class of defect that produced the 05.1-10 incident (a custom stacks directory silently not mounting), and `.planning/STATE.md` has carried it as an open blocker since 05.1-10 because that session lacked file access.

Purpose: an operator's first five minutes with this product are spent reading that header comment. If it names the wrong file, Compose's `${VAR}` interpolation never sees their configuration, their stacks directory lands somewhere they did not choose, and nothing errors.

Output: a corrected env template header, a recorded fact-by-fact audit of the deployment guide against the three files it describes, and scope item 1's todo closed with an accurate resolution.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-deployment-and-release-readiness/09-CONTEXT.md
@.planning/phases/09-deployment-and-release-readiness/09-RESEARCH.md
</context>

## Artifacts this phase produces

This plan produces **no new symbols** — it is a documentation-parity plan. Files it touches:

| Artifact | Kind | Status |
|----------|------|--------|
| `.env.example` (header comment, lines 1-7) | config template | modified |
| `.env.production` (header comment, ~line 12) | config template | modified, or blocked-and-reported |
| `docs/deployment.md` | docs | modified only if the audit finds a divergence outside `## Database schema` |
| `.planning/todos/completed/2026-08-27-document-deployment-config-clean-env-and-docker-compose.md` | planning artifact | moved from `pending/` |

<tasks>

<!-- planner-discipline-allow: .env.local -->

<task type="auto">
  <name>Task 1: Correct the env-template header so it names the file Compose actually loads</name>
  <files>.env.example, .env.production</files>
  <read_first>
    - `.env.example` — read lines 1-40 first; the header comment block is lines 1-7 and the stale instruction is on line 3.
    - `docker-compose.yml` — confirm the `env_file:` entry (around lines 22-23) still lists `.env`, so the correction targets the filename that is actually loaded today rather than a third value.
    - `docs/deployment.md` — read the quickstart around lines 38-55; it already states the correct filename and explains at length *why* the name must be exactly `.env` (Compose's top-level `${VAR}` interpolation only reads a project-root file with that literal name, never a file supplied via `env_file:`). Match that reasoning; do not invent a new explanation.
    - `.planning/STATE.md` — the `[Phase 05.1-10]` accumulated-context entry records the exact two edits this task applies and why 05.1-10 could not apply them.
  </read_first>
  <precondition>The execution environment permits writing `.env.example`. If a secret-file access guard blocks `.env.production` specifically, that file's edit is deferred per the `<action>` fallback and the rest of the task still completes; if `.env.example` itself is unwritable, halt and report.</precondition>
  <action>
    In `.env.example`, change the header comment's copy instruction (line 3) so the filename it names is `.env` instead of `.env.local`. Keep the surrounding sentence structure, the `docker-compose.yml`'s `env_file` parenthetical, and the pointer to `docs/deployment.md` intact — this is a filename correction, not a rewrite. Preferred replacement wording for line 3 onward: `Copy this file to \`.env\` (the file docker-compose.yml's \`env_file\` entry loads, and the only filename Compose's top-level ${VAR} interpolation reads)` — keep the remainder of the sentence as it already reads.

    Apply the identical correction to `.env.production`'s equivalent header line (research places it at approximately line 12; locate it by its copy instruction, not by line number).

    Fallback if `.env.production` cannot be read or written in this environment (the documented `.env*` secret-file guard, per 09-RESEARCH.md Open Question 2 and STATE.md's 05.1-10 entry): do NOT skip silently and do NOT claim the edit was made. Record in the SUMMARY under a heading naming it a remaining gap: the file path, the exact stale text, the exact replacement text, and the reason access was refused — so a developer with access applies it in one step.

    Make no other change to either file. No new variables, no default changes, no reordering.
  </action>
  <verify>
    <automated>grep -c 'env\.local' .env.example</automated>
    <fails_when>the command prints any number other than 0, or exits non-zero because the file is missing</fails_when>
    <automated>grep -c 'env_file' docker-compose.yml</automated>
    <fails_when>prints 0, meaning the env_file mechanism this correction is aligned to no longer exists and the whole premise needs re-checking</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'env\.local' .env.example` outputs exactly `0`.
    - `grep -c 'DOCKTOR_STACKS_HOST_DIR' .env.example` outputs a non-zero count — proving the correction did not truncate or otherwise damage the template body.
    - The header comment in `.env.example` still contains the substring `docs/deployment.md`, so the pointer to the full guide survived the edit.
    - Either `grep -c 'env\.local' .env.production` outputs `0`, OR the SUMMARY contains a clearly-headed remaining-gap entry naming `.env.production`, the stale text, the replacement text, and the access refusal — one of these two is true, never neither and never both.
    - `git diff --stat .env.example` shows a change confined to the header region (no added or removed variable assignment lines).
  </acceptance_criteria>
  <done>The env templates instruct operators toward the same filename docker-compose.yml loads and docs/deployment.md documents, or the one file that could not be touched is reported as an actionable gap.</done>
</task>

<task type="auto">
  <name>Task 2: Audit the deployment guide fact-by-fact against the files it describes, then close the item-1 todo</name>
  <files>docs/deployment.md, .planning/todos/completed/2026-08-27-document-deployment-config-clean-env-and-docker-compose.md</files>
  <read_first>
    - `docs/deployment.md` — full read. This is the artifact under audit.
    - `docker-compose.yml` — full read. Source of truth for service names, `env_file`, `environment:` entries, volume mounts, published ports, and the `db: condition: service_healthy` dependency.
    - `.env.example` — full read (post-Task-1 state). Source of truth for which variables exist, their defaults, and which are marked required.
    - `Dockerfile` — full read. Source of truth for the `ENV` defaults the image bakes in (`DOCKTOR_STACKS_DIR`, `DOCKTOR_FS_POLLING`, `DOCKTOR_DB_AUTO_PUSH`, `CLIENT_DIST_PATH`, `NODE_ENV`) and for the restic / docker-CLI / compose-plugin versions.
    - `.planning/todos/pending/2026-08-27-document-deployment-config-clean-env-and-docker-compose.md` — the todo being closed; its body lists the specific defects the resolution must account for one by one.
    - `09-RESEARCH.md` §"Item 1 — Deployment Documentation Drift Check" — already performed this audit once and lists what it found clean; use it as a checklist to re-confirm, not as a substitute for looking.
  </read_first>
  <action>
    Walk `docs/deployment.md` section by section and check every factual assertion it makes about the three source files. At minimum cover: the quickstart command sequence; the environment-variable reference table (variable name, requiredness, stated default) row by row against `.env.example` and `Dockerfile`'s `ENV` lines; the `DOCKTOR_STACKS_DIR`/`DOCKTOR_STACKS_HOST_DIR` pairing and its explanation of Docker-outside-of-Docker relative-bind-mount resolution; the volumes and published-ports description against `docker-compose.yml`; the statement that no `/backups` or `/data` volume exists; the backups section's claim that restic repository configuration lives in-app; and the 10-row troubleshooting table's cause/fix claims.

    Where the guide and a source file disagree, fix the guide to match the source file — the shipped configuration is authoritative, the prose is not. Where they agree, change nothing.

    Explicitly exclude the `## Database schema` section (approximately lines 213-243) from both audit and edit. Its claims about the schemaless startup step and the absent migrations directory are accurate as of this wave, and plan 09-04 rewrites that whole section after the Prisma migrate cutover lands in 09-03. Editing it here would collide with 09-04 and would document behavior that is about to change.

    Also note, without fixing it: `package.json` declares packageManager `yarn@4.13.0` while `Dockerfile` activates `yarn@4.12.0`. The guide states no yarn version as a fact, so this is not a D-01 drift — record it in the SUMMARY as an observation only. Do not bump the Dockerfile in this plan; it is out of item 1's scope and the Dockerfile is plan 09-04's file.

    Record in the SUMMARY a table with one row per audited section: section name, the source file checked against, and the verdict (`clean` or `fixed: <what>`).

    Finally, close the todo by running `gsd_run query todo complete 2026-08-27-document-deployment-config-clean-env-and-docker-compose.md` (the launcher form used elsewhere in this project) so the file is both moved to `.planning/todos/completed/` and frontmatter-stamped. Append a Resolution section to the moved file that maps each defect the todo originally lists — Dockerfile bugs, missing schema-sync step, undocumented `BETTER_AUTH_*`, dead `DOCKTOR_DATA_DIR`/`DOCKTOR_BACKUP_DIR`, the Docker-outside-of-Docker mount mismatch — to the phase and plan that actually shipped its fix (Phase 05.1 plans 05.1-03/05.1-05/05.1-08/05.1-10 and Phase 07 plan 07-01), plus the one drift this plan fixed in Task 1. Do not close any other todo.
  </action>
  <verify>
    <automated>test -f .planning/todos/completed/2026-08-27-document-deployment-config-clean-env-and-docker-compose.md && test ! -f .planning/todos/pending/2026-08-27-document-deployment-config-clean-env-and-docker-compose.md</automated>
    <fails_when>non-zero exit, meaning the todo is still filed under pending/ or absent from completed/</fails_when>
    <automated>grep -c 'Database schema' docs/deployment.md</automated>
    <fails_when>prints 0, meaning the section this task was required to leave untouched was removed</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff docs/deployment.md` contains no hunk whose context lies inside the `## Database schema` section — that section is byte-identical to its pre-plan state.
    - The SUMMARY contains an audit table with one row per audited section of `docs/deployment.md`, each naming the source file checked and a verdict of `clean` or `fixed: <description>`.
    - `test -f .planning/todos/completed/2026-08-27-document-deployment-config-clean-env-and-docker-compose.md` succeeds and `test -f .planning/todos/pending/2026-08-27-document-deployment-config-clean-env-and-docker-compose.md` fails.
    - The moved todo file's Resolution section names at least one specific plan id (matching `05\.1-0[0-9]` or `07-01`) per defect the todo's Problem section lists.
    - `ls .planning/todos/pending/*.md | wc -l` outputs a count exactly one lower than it was before this task ran (recorded in the SUMMARY both before and after) — proving no unrelated todo was disturbed.
    - The SUMMARY records the yarn `4.13.0` vs `4.12.0` observation without a corresponding change to `Dockerfile` (`git diff --stat Dockerfile` is empty for this plan).
  </acceptance_criteria>
  <done>Every factual claim the deployment guide makes outside the Database schema section is confirmed against the shipped configuration or corrected, and scope item 1's todo is closed with a resolution that traces each original defect to the plan that fixed it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → deployment config | A human reads these templates and guide, then writes real secrets and host paths into a `.env` file Compose loads. Incorrect instructions here become misconfigured production deployments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `.env.example` / `.env.production` edits | medium | mitigate | Edits are confined to comment lines. No task reads, echoes, or copies a real `.env`/`.env.production` *value* into the transcript, the SUMMARY, or a commit. The fallback path in Task 1 reports only the comment text that must change, never a variable value. |
| T-09-02 | Tampering | `docs/deployment.md` audit | low | mitigate | The audit direction is one-way: prose is corrected to match shipped config, never the reverse. No task in this plan may change `docker-compose.yml`, `Dockerfile`, or a variable assignment in `.env.example` to make the documentation true. |
| T-09-03 | Denial of Service | operator misconfiguration from a wrong filename | medium | mitigate | This plan *is* the mitigation for the live instance of this threat: an operator following the stale header lands on a filename Compose does not interpolate, silently mounting the stacks directory at the wrong host path (the 05.1-10 data-placement defect). Task 1's acceptance criteria assert the corrected filename. |
| T-09-04 | Repudiation | todo closed without evidence | low | accept | Accepted: the Resolution section requires a plan id per defect, and the pending-count assertion catches collateral closures. Deeper provenance checking is disproportionate for a documentation todo. |

No package-manager installs occur in this plan, so no supply-chain threat (`T-09-SC`) applies here; it is registered in plan 09-06, which installs `@fastify/multipart`.
</threat_model>

<verification>
- `grep -c 'env\.local' .env.example` prints `0`.
- `git diff docs/deployment.md` shows no change inside `## Database schema`.
- The item-1 todo exists only under `.planning/todos/completed/`.
- `ls .planning/todos/pending/*.md | wc -l` dropped by exactly 1.
- SUMMARY contains the per-section audit table and, if applicable, the `.env.production` remaining-gap entry.
</verification>

<success_criteria>
Scope item 1 is closed on evidence, not assumption: the one confirmed drift is fixed, the rest of the deployment guide is audited section by section against the config it describes with each verdict recorded, and the todo's resolution traces every original defect to the plan that shipped its fix.
</success_criteria>

<output>
Create `.planning/phases/09-deployment-and-release-readiness/09-01-SUMMARY.md` when done.
</output>
