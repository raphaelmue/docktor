---
phase: 09-deployment-and-release-readiness
plan: 04
type: execute
wave: 2
depends_on:
  - 09-03
files_modified:
  - Dockerfile
  - server/src/index.ts
  - .env.example
  - docs/deployment.md
  - .planning/todos/completed/2026-09-01-adopt-prisma-migrate-post-mvp.md
autonomous: true
requirements:
  - "n/a — phase scoped by four todo files, not by REQUIREMENTS.md IDs. This plan closes: .planning/todos/pending/2026-09-01-adopt-prisma-migrate-post-mvp.md (scope item 2, decisions D-02, D-04, D-05)"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-04: the production image declares the new migration opt-out variable with its default, and its inline documentation describes applying migrations rather than pushing a schema."
    - "D-04: the generated baseline migration is present inside the built image at the path the startup step resolves, so a container boot can actually apply it."
    - "D-05: the startup log an operator reads on first boot after upgrade tells them a baseline occurred, distinguishes that from a normal apply and from a no-op, and names the variable that disables the step."
    - "D-02/D-04/D-05: docs/deployment.md's Database schema section describes the shipped mechanism — migration-based apply, the automatic baseline branch and its precondition, the drift report and what to do about it, the opt-out variable and its deprecated alias, and how a developer authors a new schema change."
    - "The environment-variable reference table in both docs/deployment.md and .env.example lists the new opt-out variable with its default and requiredness, and marks the old variable as a deprecated alias rather than deleting it silently."
    - "The item-2 todo is closed with a resolution naming the baseline mechanism, the replaced startup path, the auto-baseline behaviour, and the live-verification status recorded by plan 09-03."
  prohibitions:
    - "Do not describe the automatic baseline as verified against a live database unless plan 09-03's SUMMARY says Branch A was taken — if it took Branch B, the documentation must state that live upgrade confirmation is outstanding."
    - "Do not remove the old opt-out variable from the image or the documentation — it remains a deprecated alias so an existing operator's opt-out survives the upgrade."
    - "Do not change server/src/lib/schema-sync.ts in this plan; it is plan 09-03's file and its behaviour is already settled."
    - "Do not add a member to the SchemaSyncOutcome union — server/src/index.ts's switch is exhaustive and 09-03 deliberately kept the union stable."
  artifacts:
    - "Dockerfile — DOCKTOR_DB_AUTO_MIGRATE ENV added, surrounding comment rewritten, migrations directory presence confirmed in the image layout"
    - "server/src/index.ts — startup log strings rewritten for the migration-based step, baseline case surfaced"
    - ".env.example — DOCKTOR_DB_AUTO_MIGRATE documented, DOCKTOR_DB_AUTO_PUSH marked deprecated"
    - "docs/deployment.md — Database schema section rewritten, variable table updated, troubleshooting row updated"
    - ".planning/todos/completed/2026-09-01-adopt-prisma-migrate-post-mvp.md — moved and stamped"
  key_links:
    - "Dockerfile's `COPY --from=server-build /app/server/prisma ./server/prisma` already carries server/prisma/migrations/ into the image as a side effect of copying the whole prisma directory — confirm this rather than adding a redundant COPY, and confirm .dockerignore does not exclude it."
    - "schema-sync.ts's resolvePrismaConfigPath() resolves server/prisma/prisma.config.ts from two candidate layouts (source and built image); prisma.config.ts's `schema:` then resolves the schema directory relative to itself, and the CLI finds `migrations/` as a sibling of `schema/`. If the image layout ever breaks that sibling relationship, migrations silently stop being found."
    - "syncDatabaseSchema()'s returned detail string is the only channel by which index.ts learns a baseline occurred (09-03 prefixes it rather than adding an outcome member) — index.ts's applied/already-current log must surface detail, not discard it."
---

<objective>
Make the rest of the product agree with the migration cutover plan 09-03 landed: the image, the startup logs an operator reads, the env templates, and the deployment guide.

09-03 proved the mechanism end to end on one path. This plan expands horizontally from that proven slice into every surface that describes or configures it. Right now `Dockerfile` bakes in an `ENV` whose comment explicitly promises the schemaless step and points at the very todo this phase is closing, `server/src/index.ts` logs a variable name that no longer controls anything, and `docs/deployment.md` tells operators the migrations directory does not exist.

Purpose: a deployment step that works but is documented as something else is a support burden and, on the auto-baseline path specifically, a safety issue — an operator who does not know a baseline happened cannot act on a drift report.

Output: an image and a startup log that describe what actually runs, env templates and a deployment guide that document the new variable and the upgrade path honestly, and scope item 2's todo closed.

**Tracer note:** the vertical slice for scope item 2 was plan 09-03. This plan is deliberately its horizontal expansion — documentation and configuration surfaces around an already-proven path — so it carries no tracer task of its own.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-deployment-and-release-readiness/09-CONTEXT.md
@.planning/phases/09-deployment-and-release-readiness/09-RESEARCH.md
@.planning/phases/09-deployment-and-release-readiness/09-03-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Status |
|----------|------|--------|
| `DOCKTOR_DB_AUTO_MIGRATE` | `ENV` declaration in `Dockerfile`, default `true` | new |
| `DOCKTOR_DB_AUTO_PUSH` | `ENV` declaration in `Dockerfile` | retained, re-documented as a deprecated alias |
| `## Database schema` | section of `docs/deployment.md` | rewritten |
| `.planning/todos/completed/2026-09-01-adopt-prisma-migrate-post-mvp.md` | planning artifact | moved from `pending/` |

No new source symbols, exports, or schema fields. `server/src/index.ts` changes are log strings and comments only — its control flow and its exhaustive switch over `SchemaSyncOutcome` stay as they are.

<tasks>

<!-- planner-discipline-allow: todos/pending/2026-09-01-adopt-prisma-migrate-post-mvp.md -->
<!-- planner-discipline-allow: does not exist -->

<task type="auto">
  <name>Task 1: Make the image and the startup log describe the migration step that actually runs</name>
  <files>Dockerfile, server/src/index.ts, .env.example</files>
  <read_first>
    - `Dockerfile` — full read. The final stage's `ENV` block declares `DOCKTOR_DB_AUTO_PUSH=true` with a comment that names the schemaless command and links the todo this phase closes. Also note `COPY --from=server-build /app/server/prisma ./server/prisma` in the final stage — this is what carries the schema (and now the migrations) into the image.
    - `.dockerignore` — confirm nothing in it excludes `server/prisma/migrations` or a `*.sql` pattern that would strip the baseline out of the build context.
    - `server/src/index.ts` — full read. The `switch (schemaSyncResult.outcome)` block and the comment above the `syncDatabaseSchema()` call both name the schemaless step and the old variable.
    - `server/src/lib/schema-sync.ts` — post-09-03 state. You are NOT editing it; read it to learn the exact opt-out variable names it now checks, the `detail` prefix it uses when a baseline occurred, and the outcome members it still returns, so the log strings you write are true.
    - `.planning/phases/09-deployment-and-release-readiness/09-03-SUMMARY.md` — which branch Task 3 of 09-03 took. This determines whether documentation may claim live verification.
    - `.env.example` — read the Server section and the surrounding comment style (each variable gets a Required/Optional line plus a default and a prose rationale). Match it.
  </read_first>
  <action>
    **Dockerfile.** Add `ENV DOCKTOR_DB_AUTO_MIGRATE=true` alongside the existing `ENV DOCKTOR_DB_AUTO_PUSH=true`. Keep the old declaration — removing it would drop the deprecated-alias opt-out for an operator whose compose file or environment still sets it, and `schema-sync.ts` still reads it. Rewrite the comment block above them to state: the container applies pending Prisma migrations on startup before the HTTP server listens, so a fresh `docker compose up` against an empty database creates the schema rather than crashing on a missing table; an existing install whose schema was created by the older schemaless mechanism is detected and baselined into migration history automatically on first boot after upgrade; `DOCKTOR_DB_AUTO_MIGRATE=false` disables the whole step; `DOCKTOR_DB_AUTO_PUSH=false` is honoured as a deprecated alias for operators upgrading from a version that used it. Delete the sentence promising the schemaless behaviour and the pointer to `.planning/todos/pending/2026-09-01-adopt-prisma-migrate-post-mvp.md`; after this phase that path does not exist.

    Confirm — do not assume — that the baseline migration reaches the image. `COPY --from=server-build /app/server/prisma ./server/prisma` copies the whole `server/prisma` directory, and `server/prisma/migrations/` is a sibling of `server/prisma/schema/`, so it should be included by the existing COPY. Verify by building the image (or, if a build is unavailable, by inspecting `.dockerignore` and the COPY lines) and record the evidence in the SUMMARY. If and only if it is genuinely excluded, add an explicit COPY — do not add a redundant one otherwise. Also confirm nothing in `.dockerignore` filters `*.sql`.

    Leave the yarn version line alone. `package.json` pins `yarn@4.13.0` while this file activates `yarn@4.12.0` — plan 09-01 recorded that as an observation, and bumping it here would mix an unrelated build change into a documentation plan.

    **server/src/index.ts.** Rewrite the comment above the `syncDatabaseSchema()` call and every string inside the outcome `switch`, so an operator reading container logs learns the truth:
    - The `applied` and `already-current` cases already log `detail`. Keep doing so — 09-03 prefixes `detail` with a baseline marker rather than adding an outcome member, so `detail` is the only channel that reveals a baseline happened. Do not discard, truncate, or reformat it away.
    - The `skipped` case currently hardcodes the old variable name in its message. Change it to name `DOCKTOR_DB_AUTO_MIGRATE` and mention that the older variable is still honoured as a deprecated alias.
    - The `failed` case currently advises setting the old variable. Change it to advise `DOCKTOR_DB_AUTO_MIGRATE=false` and to state that a failed migration means pending migrations were not applied, so the instance is running against a schema that may be missing tables or columns.
    - The `unreachable` and `lock-not-acquired` cases need only wording that no longer describes a schema push; their semantics are unchanged.

    Do not change the control flow, the ordering relative to `assertStacksDirMatchesHost()` / `ensureStacksDir()` / `assertStacksDirIsMounted()` / `buildApp()`, or the `SchemaSyncOutcome` union.

    **.env.example.** Add a `DOCKTOR_DB_AUTO_MIGRATE` entry in the same position and comment style as the surrounding variables: mark it Optional with default `true`, state that it applies pending migrations at container startup and auto-baselines a pre-migration install, and point at the Database schema section of `docs/deployment.md`. If `DOCKTOR_DB_AUTO_PUSH` appears in this file, re-label it as a deprecated alias retained for upgrades rather than deleting it. Change no variable's value.
  </action>
  <verify>
    <automated>grep -c 'DOCKTOR_DB_AUTO_MIGRATE' Dockerfile server/src/index.ts .env.example</automated>
    <fails_when>any of the three listed files reports a count of 0, or the command exits non-zero because a file is missing</fails_when>
    <automated>grep -c 'DOCKTOR_DB_AUTO_PUSH' Dockerfile</automated>
    <fails_when>prints 0 — the deprecated-alias declaration was deleted, silently revoking an upgrading operator's existing opt-out</fails_when>
    <automated>! grep -rq 'todos/pending/2026-09-01-adopt-prisma-migrate-post-mvp' Dockerfile server/src/index.ts</automated>
    <fails_when>non-zero exit, meaning a stale pointer to the now-closed todo survives in the image definition or the startup file</fails_when>
    <automated>yarn typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ENV DOCKTOR_DB_AUTO_MIGRATE=true' Dockerfile` outputs `1`, and `grep -c 'DOCKTOR_DB_AUTO_PUSH' Dockerfile` outputs a non-zero count.
    - `grep -rc 'todos/pending/2026-09-01-adopt-prisma-migrate' Dockerfile server/src/index.ts` outputs `0` for both files.
    - `server/src/index.ts`'s `skipped` log string contains `DOCKTOR_DB_AUTO_MIGRATE`, and its `failed` log string contains `DOCKTOR_DB_AUTO_MIGRATE`.
    - The `applied` and `already-current` cases in `server/src/index.ts` still interpolate `schemaSyncResult.detail` — verified by `grep -c 'schemaSyncResult.detail' server/src/index.ts` outputting a count of at least 2.
    - `git diff server/src/index.ts` shows no change to the ordering of `assertStacksDirMatchesHost()`, `ensureStacksDir()`, `assertStacksDirIsMounted()`, `syncDatabaseSchema()`, `buildApp()`, or `app.listen()`, and no `case` label added to or removed from the switch.
    - `git diff --stat server/src/lib/schema-sync.ts` is empty for this plan.
    - The SUMMARY records the evidence that `server/prisma/migrations/` is present in the built image (build output, image inspection, or the COPY-plus-`.dockerignore` analysis), and states whether an explicit COPY was needed.
    - `yarn typecheck` exits 0 with no `error TS` line.
    - `git diff Dockerfile` contains no change to the `corepack prepare yarn@` lines.
  </acceptance_criteria>
  <done>An operator reading the image definition or the first-boot container log learns that migrations are applied, that a pre-migration install baselines itself, and which variable turns the step off — all of which are true.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the deployment guide's schema section for the migration mechanism and close the item-2 todo</name>
  <files>docs/deployment.md, .planning/todos/completed/2026-09-01-adopt-prisma-migrate-post-mvp.md</files>
  <read_first>
    - `docs/deployment.md` — full read, with attention to the `## Database schema` section (approximately lines 213-243), the environment-variable reference table (approximately lines 110-130), the first-boot log expectation around line 101, and the troubleshooting table's row about an empty database crashing on a missing table (approximately line 265).
    - `server/src/lib/schema-sync.ts` — post-09-03 state. The guide must describe this file's actual behaviour: which guards run, in what order, what the baseline branch requires, what the drift probe reports.
    - `server/src/index.ts` — post-Task-1 state, so the guide's description of first-boot log lines matches the strings that are actually emitted.
    - `package.json` — post-09-03 state. `db:push` is gone, `db:migrate` remains; the guide must tell developers which command now authors a schema change.
    - `.planning/phases/09-deployment-and-release-readiness/09-03-SUMMARY.md` — the branch Task 3 of 09-03 took. If Branch B, the guide and the todo resolution must say live upgrade confirmation is outstanding.
    - `.planning/todos/pending/2026-09-01-adopt-prisma-migrate-post-mvp.md` — the todo being closed; its body states the original rationale the resolution must address.
    - `.planning/phases/09-deployment-and-release-readiness/09-01-SUMMARY.md` — plan 09-01 audited this same file in the previous wave and deliberately excluded this section. Read its audit table so you do not undo a correction it made.
  </read_first>
  <action>
    **Rewrite `## Database schema`.** Replace the whole section. Delete the claim that the project uses schemaless synchronisation, the claim that `server/prisma/migrations/` does not exist, and the pointer to the now-closed todo. The replacement must cover, in operator-facing prose:

    - What runs at container startup: pending Prisma migrations are applied before the HTTP server listens, from `server/prisma/migrations/`, orchestrated by `server/src/lib/schema-sync.ts` wired into `server/src/index.ts`.
    - The four guards, which are unchanged from the previous step and worth keeping in the guide because operators rely on them: skipped entirely when the opt-out variable is `false`; reachability retried for roughly a minute so a still-starting Postgres is tolerated; a Postgres advisory lock so two replicas starting together cannot both apply; never a data-loss-acceptance or reset flag; and never fatal to server startup, so a failure is logged and the instance is still reachable.
    - The upgrade path for an existing install, stated plainly: if the database already has Docktor's tables but no migration-history table (because an earlier version created the schema with the older mechanism), the startup step records the baseline migration `0_init` as already applied and then proceeds. No operator action is required. Immediately afterwards it probes the live schema for residual divergence, and reports any divergence loudly in the logs.
    - What to do when that drift report appears: it means the database's schema was behind the shipped schema at upgrade time, so the automatic baseline recorded more than the database actually has. Give the operator the recovery path — inspect the reported divergence, apply the missing changes deliberately, or set the opt-out variable and manage the schema themselves. Do not promise an automatic repair; there is none, by design.
    - A fresh, empty database: no baseline is recorded; the migration is applied cold.
    - The opt-out: `DOCKTOR_DB_AUTO_MIGRATE=false`, with `DOCKTOR_DB_AUTO_PUSH=false` honoured as a deprecated alias so an existing opt-out survives the upgrade.
    - How a developer authors a schema change from now on: edit the relevant file under `server/prisma/schema/`, then run `yarn db:migrate` (which runs Prisma's dev migration command and regenerates the client). Note that the schemaless convenience script was removed, so schema changes now always produce a reviewable migration file.
    - Live-verification honesty: if 09-03's SUMMARY records Branch B, add a short, clearly-marked note that the automatic upgrade baseline has not yet been exercised against a live database in this project's own environment and name what remains. If Branch A, say nothing about it — the behaviour is simply documented as shipped.

    **Update the environment-variable reference table.** Add a `DOCKTOR_DB_AUTO_MIGRATE` row (Optional, default `true`, description pointing at the Database schema section) and re-describe the existing `DOCKTOR_DB_AUTO_PUSH` row as a deprecated alias retained for upgrades. Keep the table's existing column structure and row ordering conventions.

    **Update the first-boot expectation** (around line 101) so the `[schema-sync]` line it tells operators to look for matches the strings `server/src/index.ts` now emits, including the baseline case.

    **Update the troubleshooting row** about an empty database crashing on a missing table so its Fix column names the migration step and `DOCKTOR_DB_AUTO_MIGRATE` rather than the old variable. Leave the other nine rows alone.

    Change nothing else in this file. Plan 09-01 audited the rest of it in the previous wave; re-editing audited prose here risks undoing a correction it made.

    **Close the todo.** Run `gsd_run query todo complete 2026-09-01-adopt-prisma-migrate-post-mvp.md` so the file is moved to `.planning/todos/completed/` and frontmatter-stamped; do not hand-roll a `git mv`. Append a Resolution section stating: the baseline was generated with Prisma's own diff command from the current modular schema directory rather than hand-written; the guarded startup step now applies migrations and the schemaless path was removed from both the runtime and the root scripts (D-04); an upgrading install auto-baselines at boot with a drift probe behind it (D-05); the original "as late as possible" rationale was superseded by D-02 with the v1.0.0 release as the stated trigger; and the live-verification status exactly as 09-03's SUMMARY records it. Close no other todo.
  </action>
  <verify>
    <automated>grep -c 'DOCKTOR_DB_AUTO_MIGRATE' docs/deployment.md</automated>
    <fails_when>prints 0 — the new opt-out variable is undocumented</fails_when>
    <automated>grep -c '0_init' docs/deployment.md</automated>
    <fails_when>prints 0 — the baseline migration an upgrading operator will see recorded in their database is never named in the guide</fails_when>
    <automated>grep -c 'db:migrate' docs/deployment.md</automated>
    <fails_when>prints 0 — the guide does not tell a developer how to author a schema change now that the schemaless script is gone</fails_when>
    <automated>test -f .planning/todos/completed/2026-09-01-adopt-prisma-migrate-post-mvp.md && test ! -f .planning/todos/pending/2026-09-01-adopt-prisma-migrate-post-mvp.md</automated>
    <fails_when>non-zero exit, meaning the todo is still filed under pending/ or absent from completed/</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/deployment.md`'s `## Database schema` section contains all of: `DOCKTOR_DB_AUTO_MIGRATE`, `DOCKTOR_DB_AUTO_PUSH`, `0_init`, `yarn db:migrate`, and `server/prisma/migrations/`.
    - The `## Database schema` section makes no claim that the migrations directory is absent: read the section and confirm every sentence mentioning `server/prisma/migrations` describes it as present and as the source the startup step applies from. Record the checked sentences in the SUMMARY.
    - `grep -rc 'todos/pending/2026-09-01-adopt-prisma-migrate' docs/deployment.md` outputs `0`.
    - The environment-variable reference table contains a row whose first cell names `DOCKTOR_DB_AUTO_MIGRATE` with default `true`, and the `DOCKTOR_DB_AUTO_PUSH` row's description contains the word `deprecated`.
    - The Database schema section describes the drift-report condition and gives at least one concrete operator action for it — it does not merely mention that a probe exists.
    - If `09-03-SUMMARY.md` records Branch B, `docs/deployment.md` contains a note stating the automatic upgrade baseline is not yet live-verified; if it records Branch A, no such note is present. The SUMMARY states which branch applied and therefore which of these two is expected.
    - `git diff docs/deployment.md` touches only the Database schema section, the variable reference table, the first-boot log expectation, and the one troubleshooting row — no other section.
    - `test -f .planning/todos/completed/2026-09-01-adopt-prisma-migrate-post-mvp.md` succeeds and the pending copy is gone; the Resolution section names D-02, D-04, and D-05 and records the live-verification status.
    - `ls .planning/todos/pending/*.md | wc -l` is exactly one lower than before this task ran (both counts recorded in the SUMMARY).
  </acceptance_criteria>
  <done>The deployment guide describes the migration mechanism that actually ships, tells an upgrading operator what will happen to their database and what to do if the drift report fires, tells a developer how to author the next schema change, and scope item 2's todo is closed against that record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator action | Operators decide whether to disable unattended schema mutation based on what this guide says. Wrong or missing information here produces wrong decisions about a production database. |
| image definition → container runtime | `ENV` defaults in the image are the effective configuration for every operator who does not override them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-16 | Repudiation | undocumented auto-baseline | high | mitigate | An operator whose database is silently baselined and then diverges has no way to interpret the drift log line unless the guide explains it. Mitigated by the required Database schema content: the baseline precondition, the drift report, and at least one concrete recovery action, all asserted in acceptance criteria. Task 1 additionally requires `detail` to keep reaching the startup log, which is where the baseline marker appears. |
| T-09-17 | Tampering | deprecated opt-out silently dropped from the image | medium | mitigate | The old `ENV` declaration is explicitly retained and an acceptance criterion asserts it is still present in `Dockerfile`; the guide's variable table must describe it as deprecated rather than omit it. An operator who set it to `false` keeps their opt-out through the upgrade. |
| T-09-18 | Denial of Service | baseline migration missing from the built image | high | mitigate | If `server/prisma/migrations/` does not reach the image, every container boot applies nothing and a fresh install crashes on missing tables — the exact defect the startup step exists to prevent. Task 1 requires positive evidence of the directory's presence in the image (build output or image inspection, not assumption) plus a `.dockerignore` check, recorded in the SUMMARY. |
| T-09-19 | Information Disclosure | secrets in documentation examples | low | mitigate | No task in this plan adds a variable value, only a variable name, a default, and prose. `.env.example` edits are confined to a new Optional entry and a re-labelling comment; no existing assignment changes. |
| T-09-20 | Spoofing | documentation claiming unverified live behaviour | medium | mitigate | The guide's live-verification note is conditioned directly on 09-03's recorded branch, and an acceptance criterion asserts the note's presence or absence matches that branch. Documentation cannot claim a live upgrade was proven when it was not. |

No package-manager install occurs in this plan, so `T-09-SC` does not apply here; it is registered in plan 09-06.
</threat_model>

<verification>
- `Dockerfile` declares `DOCKTOR_DB_AUTO_MIGRATE=true`, retains `DOCKTOR_DB_AUTO_PUSH`, and carries no pointer to the closed todo.
- `server/src/index.ts` names the new variable in its `skipped` and `failed` log strings, still surfaces `detail` on `applied`/`already-current`, and has an unchanged startup ordering and switch shape.
- `yarn typecheck` exits 0.
- SUMMARY records positive evidence that `server/prisma/migrations/` is inside the built image.
- `docs/deployment.md`'s Database schema section names the new variable, the deprecated alias, `0_init`, `yarn db:migrate`, the drift report and its recovery action, and matches 09-03's live-verification branch.
- The item-2 todo lives only under `.planning/todos/completed/` with a resolution naming D-02, D-04, D-05 and the live-verification status; the pending count dropped by exactly 1.
</verification>

<success_criteria>
Every surface that configures or describes Docktor's schema management — the image, the startup log, the env templates, and the deployment guide — tells the same true story as the code plan 09-03 shipped, including the honest state of live verification, and scope item 2's todo is closed against that record.
</success_criteria>

<output>
Create `.planning/phases/09-deployment-and-release-readiness/09-04-SUMMARY.md` when done.
</output>
