---
phase: 05.1-stabilization-fix-blockers-and-majors-surfaced-during-testin
verified: 2026-09-02T13:57:30Z
status: human_needed
score: 20/22 must-haves verified
behavior_unverified: 0
overrides_applied: 0
requirements_traceability: n/a — phase declares no REQUIREMENTS.md IDs (confirmed no-op: grep of REQUIREMENTS.md traceability table shows no Phase 05.1 row)
human_verification:
  - test: "Run `yarn workspace @docktor/server test:integration` on a host without the sandbox's TCP-payload-to-Docker-published-port restriction"
    expected: "Exits 0 across all 4 integration test files (setup-concurrency, setup-wizard-flow, stacks, imports)"
    why_human: "Independently reproduced in this verification session: all 4 integration files fail identically with P1001 / 'prismaClient is not initialised', while 499 unit tests pass cleanly. This is a confirmed host-level TCP payload-delivery block to Docker-published ports (SYN-ACK completes, no data flows), documented consistently across 3 independent sessions (05.1-01, 05.1-05, and this verifier's own run) and in WINDOWS.md items #1, #2, #9. Not a code defect — cannot be resolved by grep/read."
  - test: "With two browser tabs open on the same stack, press Deploy/Stop/Restart/Update in one tab and confirm the status badge updates live in both tabs; save compose/env in one tab and confirm the config-changed banner appears live in both"
    expected: "Both tabs reflect the SSE-broadcast status/config-changed state without a manual reload"
    why_human: "No running Docktor instance/browser available in any execution session to date. Underlying unit-level behavior (broadcaster call sites, non-throwing guard, refetch-on-save) is fully proven by passing unit tests (stack-service.test.ts, backup-service.test.ts, stack-detail-page.test.tsx, use-stack.test.ts) — this is the live cross-tab UX confirmation only a human with a running instance can give. WINDOWS.md #3."
  - test: "Edit a running stack's .env file directly on disk and confirm the config-changed badge appears within the watcher's detection window; introduce a YAML syntax error into a running stack's compose file and confirm a red config-error indicator appears on both the stack list and detail page without a manual reload, then confirm it clears when the file is fixed"
    expected: "Both indicators appear/clear live, driven by FileWatcher + SSE + the new UI badges"
    why_human: "No running Docktor instance/browser available. All underlying unit-level behavior verified (42 file-watcher tests + hook/UI tests independently re-run and passing in this session). WINDOWS.md #6, #7."
  - test: "Create a stack with a relative bind-mount volume (e.g. `./volumes/data:/var/opt/app`) through Docktor's own deploy pipeline, in an isolated Docker host with no unrelated running projects, and confirm the data lands at the identical host path"
    expected: "Host-side directory appears under the stack's own directory, matching the container-side write"
    why_human: "The mechanism was proven live once in a prior session (05.1-03) via a manual docker-compose invocation that mirrored DockerExecutor's cwd behavior, but that same test caused a real incident on the shared host (stopped a running unrelated 'memos' stack via --remove-orphans project-name collision) and was not repeated through Docktor's own pipeline. Deliberately not re-attempted in this verification session for the same host-safety reason. WINDOWS.md #4."
  - test: "Run an actual restic backup, then a restore, against a real deployed stack, and confirm the restored files match what the containers wrote"
    expected: "Backup archive and restored files are byte-identical to the live stack directory"
    why_human: "D2 in 05.1-03 was confirmed by reading BackupService's call sites (stack.hostPath passed to restic), not by an executed backup+restore cycle. WINDOWS.md #5."
  - test: "Run `server/test/integration/imports.test.ts` on an unrestricted host and confirm all 4 unauthenticated-request tests return 401, the scan+adopt round trip succeeds, and the T-05-09 410 regression guard still holds"
    expected: "All assertions pass against a real Postgres-backed Fastify instance"
    why_human: "Same environmental TCP-payload block as above; the test file exists, was code-reviewed against passing sibling test files, and tsc is clean, but was never executed to green. WINDOWS.md #9."
---

# Phase 05.1: Stabilization — Verification Report

**Phase Goal:** Fix the blockers and majors surfaced during Phase 1-5 testing/UAT that block a clean, safe self-hosted deployment, so Phase 6 (Proxy Configuration) starts from a working, documented, verifiable base. Scope is exactly 10 items: 3 blockers (broken integration/e2e tests, no schema sync on container startup, Docker-outside-of-Docker bind-mount path mismatch), 6 majors (deployment config documentation, `config_error` UI, missing SSE broadcasts on manual actions, backup-without-repo wedging a stack, env edits not flagging config-changed, unreachable post-setup brownfield import) and 1 minor (restic pinned to an outdated version).

**Verified:** 2026-09-02T13:57:30Z
**Status:** human_needed
**Re-verification:** No — initial verification (no prior VERIFICATION.md existed for this phase)

## Requirements Traceability

Confirmed no-op per task instructions: `REQUIREMENTS.md`'s traceability table (lines 119-173) has no row for Phase 05.1 — every v1 requirement maps to Phases 1-6, and Phase 05.1's own PLAN frontmatter across all 8 plans declares `requirements: []`. This phase is correctly scoped by the todo list, not REQUIREMENTS.md IDs. Nothing further to check here.

## Methodology note

This verification independently reproduced several claims rather than trusting SUMMARY.md narrative:
- Ran `yarn workspace @docktor/server tsc --noEmit` and `yarn workspace @docktor/client tsc --noEmit` — both clean.
- Ran the full server unit+integration suite (`yarn workspace @docktor/server test --run`): **499 unit tests passed**, 4 integration test files failed identically with the documented P1001/"prismaClient is not initialised" error — independently corroborating the environmental TCP-payload-block claim made across 3 different plan SUMMARYs (05.1-01, 05.1-05, 05.1-07) rather than accepting it on narrative alone.
- Ran the full client unit suite in parallel (matches the flaky pattern the SUMMARYs describe under this host's CPU contention: 13/137 timeouts), then re-ran the specific failing files in isolation — `stack-detail-page.test.tsx` (4/4 pass) and `service-upgrade-dialog.test.tsx` (9/9 pass) — confirming the failures are host contention, not code defects, exactly as documented.
- Read and confirmed, directly in the current working tree, the code for all 6 code-review fixes (CR-01, WR-01 through WR-05) claimed fixed in `05.1-REVIEW-FIX.md`, and confirmed all 6 fix commits exist in `git log`.
- Attempted to read `.env.example` directly via both Bash and the Read tool — both denied by filesystem permission settings in this session too, independently corroborating the same restriction multiple plan SUMMARYs (05.1-03, 05.1-07, 05.1-08) reported for `.env*` files.
- Confirmed zero uncommitted source changes (`git status --short` shows only `.planning/` metadata and untracked `.gsd/`), and zero debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) in the phase's modified source files.

## Goal Achievement

### Observable Truths (merged from ROADMAP scope items + all 8 plans' `must_haves.truths`)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | (B1) Client Playwright e2e suite passes end-to-end | ✓ VERIFIED | `client/playwright.config.ts` has `reporter: 'list'`, `workers: 1`; `_apiRouteGuard` fixture present in `client/test/integration/fixtures.ts`; specs for stacks/backups/setup-wizard/stacks-import/auth exist with plausible test counts. Documented passing runs (69/69, then 71/71) across 2 independent plan sessions with per-test detail; not independently re-run in this verification session (Playwright run is multi-minute on this host — outside "keep verification fast" budget), but strong corroborating static evidence. |
| 2 | (B1) Server integration suite exits 0 | ✗ human_needed | Independently reproduced failing in this verification session: 4/35 test files fail with P1001, identical error signature documented in 05.1-01/05.1-05/05.1-07. 499/499 unit tests pass. Confirmed environmental (host TCP-payload-to-Docker-port block), not a code defect — see human_verification. |
| 3 | (B1) `startContainer()` failure surfaces real cause, not a downstream TypeError | ✓ VERIFIED | `server/test/integration/setup.ts:106,116` — named error messages present; `execFileSync` (argv array) replaces shell-interpolated `execSync`. |
| 4 | (B1) Integration harness never leaks testcontainers Postgres containers | ✓ VERIFIED | `setup.ts:91` `AggregateError`-wrapped `stopContainer()`; all 3 pre-existing integration test files wrap `cleanDatabase()` in `try/finally`. Reproducible mechanism (my own integration run exercised the exact failing path and did not hang or crash uncontrolled). |
| 5 | (B2) Fresh `docker compose up` against an empty DB serves `GET /api/setup/status` successfully | ✓ VERIFIED | `server/src/lib/schema-sync.ts` implemented (8/8 unit tests independently re-run, passing); wired in `server/src/index.ts` before `app.listen()`. A real bug (`--skip-generate`, rejected by Prisma CLI 7.4.0) was found and fixed live in 05.1-08 with a documented end-to-end reproduction (`[schema-sync] applied`, `GET /api/setup/status` → 200, admin account created via `POST /api/setup/step1`) — this closes the loop that 05.1-05 itself could not close. New regression test asserts `--skip-generate` is never in argv (`grep -n skip-generate server/src/lib/schema-sync.ts` confirms the comment and absence in `buildArgv()`). |
| 6 | (B2) Second start against an already-current DB is a no-op, logs "already in sync" | ✓ VERIFIED | Unit-tested (`schema-sync.test.ts`); regex matched against real prior CLI output text per 05.1-05's key-decisions. |
| 7 | (B2) Startup schema step never accepts data loss / never resets | ✓ VERIFIED | `buildArgv()` unit test asserts no data-loss/reset flag present; code read confirms argv is `['db','push','--config=...']` only. |
| 8 | (B2) Two instances starting simultaneously don't both apply the schema | ✓ VERIFIED | Postgres advisory lock (`pg_try_advisory_lock`) unit-tested (lock-not-acquired case). |
| 9 | (B2) Operator can disable the startup schema step with one env var | ✓ VERIFIED | `DOCKTOR_DB_AUTO_PUSH` opt-out unit-tested; documented in `docs/deployment.md`. |
| 10 | (B3) A relative bind mount resolves to the identical absolute path on host and in-container | ✓ VERIFIED (code) / human_needed (live regression) | `docker-compose.yml` stacks volume: `${DOCKTOR_STACKS_HOST_DIR:-/opt/docktor/stacks}:${DOCKTOR_STACKS_HOST_DIR:-/opt/docktor/stacks}` — read directly, source==target. Live-proven once in 05.1-03 but the live test method caused a real incident on the shared host (stopped an unrelated running "memos" stack) and needs re-verification in an isolated host — see human_verification. |
| 11 | (B3) Restic backs up the same physical host directory containers write into | ✓ VERIFIED (code inspection only) | Confirmed by reading `BackupService.runBackup` passing `stack.hostPath`; not exercised via an actual backup+restore cycle — flagged as human_needed. |
| 12 | (B3) A host/container stacks-path mismatch refuses to start, naming the cause | ✓ VERIFIED | `assertStacksDirMatchesHost()` in `server/src/lib/stacks-dir.ts`, called from `index.ts` before `app.listen()`; unit-tested (6 cases, independently re-run: 71 tests across stacks-dir/schema-sync/file-watcher/disk-checker all passing). |
| 13 | (N1) Restic pinned to an explicit, checksum-verified release | ✓ VERIFIED | `Dockerfile` restic-install stage: `RESTIC_VERSION=0.19.1`, downloads `SHA256SUMS`, `sha256sum -c` gate — read directly, confirmed fail-closed single-`RUN` structure. |
| 14 | (M3) Every manual stack action (deploy/stop/restart/update) broadcasts `stack_status` over SSE while in flight | ✓ VERIFIED | `stack-service.ts` private `transitionStatus()` wrapper is sole call site for `repo.transitionStatus()`; unit-tested (deploy/stop/restart/updateImages/upgradeServiceImage, including identical-from/to case). Live two-tab UX not confirmed — human_needed (separate truth #21). |
| 15 | (M3) Backup/restore transitions also broadcast `stack_status` | ✓ VERIFIED | `backup-service.ts` `writeStackStatus()` wrapper is sole call site; 76/76 `backup-service.test.ts` tests independently re-run, passing. |
| 16 | (M4) A backup/restore request with no configured repository fails fast, mutates no state, never wedges the stack | ✓ VERIFIED | `initiateBackup()`/`initiateRestore()` both throw `BadRequestError` immediately after `getBackupRepoConfig()` returns null, before any row/status write — read directly in code; unit-tested. |
| 17 | (M5a) Env-only edits through the app flag `configChanged`, matching compose-edit behaviour | ✓ VERIFIED | `updateStack()`'s env branch flags `configChanged`+publishes `config_changed`; unit-tested (5 tests in `updateStack` describe block). |
| 18 | (M5b) External `.env` edits are detected by FileWatcher independent of compose-change detection | ✓ VERIFIED | `Stack.lastEnvHash` field present in `stack.prisma`; `handleEnvChange()`/exact-basename chokidar filter read directly; 71 unit tests (including file-watcher's 42) independently re-run, passing. Live detection-window confirmation not done — human_needed. |
| 19 | (M2) `config_error` is persisted, has a clear/set lifecycle, and is rendered in the UI (list pill + detail Alert) | ✓ VERIFIED | `Stack.configError` field present in schema; `clearConfigChanged()` also nulls it; UI wiring confirmed by direct grep — `stack-list.tsx:31-35` (red pill), `[id].tsx:215-222` (destructive Alert); `use-stacks.ts`/`use-stack.ts` both handle the `config_error` SSE event type. |
| 20 | (M6) An authenticated user can reach brownfield scan/adopt/migrate from the Stacks page post-setup, through auth-gated routes independent of `/api/setup/*` | ✓ VERIFIED | `server/src/routes/imports.ts` — `app.addHook("onRequest", requireAuth)` is the plugin's first statement (read directly); `client/src/routes/app/stacks/import.tsx` + Stacks-page entry point present; e2e (`stacks-import.spec.ts`) exists and was documented passing (71/71 full suite). Server-side 401/410 integration assertions exist in `imports.test.ts` but could not execute (same P1001 block) — human_needed. |
| 21 | (M1) `.env.example` documents every server-read env var truthfully, no dead config; `docker-compose.yml` is the final self-host deployment; `docs/deployment.md`/`README.md` exist and are accurate | ✓ VERIFIED | `docker-compose.yml`/`docker-compose.dev.yml` read directly (postgres:18 aligned, stacks volume identical-path, no hardcoded DB password post-WR-04). `docs/deployment.md` documents `POSTGRES_PASSWORD` requirement (`grep` confirms lines 47, 114). `.env.example` itself could not be read in this session (filesystem permission denial, independently reproduced) — treated as unverified-by-me but the deployment quickstart it feeds was live-verified end-to-end in 05.1-08 with concrete evidence (schema applied, SPA served, admin account created). |
| 22 | Manual UAT: two-tab live SSE confirmation, live `.env`/compose-error UI confirmation | UNVERIFIED (human) | No running instance/browser available in any session to date — see human_verification. |

**Score:** 20/22 truths verified (1 confirmed-environmentally-blocked, 1 requiring live-browser confirmation folded into the human_verification list; several VERIFIED truths carry an accompanying, separately-tracked human-verification item for full end-to-end confidence — see below).

### Code Review Findings (05.1-REVIEW.md → 05.1-REVIEW-FIX.md)

All 6 in-scope findings (1 Critical, 5 Warning) independently re-confirmed fixed in the current working tree, not just trusted from the fix report:

| ID | Finding | Fix Verified In Code | Commit |
|----|---------|----------------------|--------|
| CR-01 | `initiateRestore()` had no status-transition guard — concurrency/data-corruption risk | `assertTransition(stack.status, "RESTORE")` present; `RESTORE` allowed-from list widened to `["RUNNING","HEALTHY","UNHEALTHY","STOPPED","ERROR"]` in `stack-status-machine.ts:19`, excluding transitional states | `ecc7f4a` |
| WR-01 | Breadcrumb link used a query string the router doesn't consume | `[id].tsx:176` now builds `/stacks/${id}/${activeTab}` (path form) | `fb38034` |
| WR-02 | `backupStackRepo.update` bypassed the repository layer with an unjustified `as` cast | `StackRepository.updateStatusFields()` added (`stack-repository.ts:396`); `application/index.ts:33-42` now calls it, cast is a real runtime narrowing with a justifying comment | `6244791` |
| WR-03 | Docker CLI/Compose plugin installed without checksum verification | `Dockerfile` now `sha256sum -c`-verifies both the pinned CLI tarball (hardcoded SHA256) and Compose plugin (via published `checksums.txt`) | `7d60b44` |
| WR-04 | Hardcoded weak default Postgres password in `docker-compose.yml` | `docker-compose.yml:70-78` — `POSTGRES_PASSWORD` no longer hardcoded, now sourced from `env_file: .env.local`; `docs/deployment.md` documents the required manual addition | `d13afcf` |
| WR-05 | `runRestoreProcess()` silently continued with an empty env object if the repo became unconfigured mid-flight | Re-checks `getBackupRepoConfig()` and throws `BadRequestError` before calling restic — read directly at `backup-service.ts:363-369` | `84d3b0a` |

All 6 fix commits confirmed present in `git log`. Info findings IN-01/IN-02 were correctly left unfixed (out of fix-scope, `critical_warning` tier only).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `server/test/integration/setup.ts` | Surfaces real failure cause, no container leak | ✓ VERIFIED | Read directly; named errors + `AggregateError` teardown present |
| `client/playwright.config.ts` | `list` reporter, `workers: 1` | ✓ VERIFIED | Read directly |
| `server/src/lib/schema-sync.ts` | Guarded startup schema step | ✓ VERIFIED | 8 unit tests independently re-run, passing |
| `docker-compose.yml` | Identical host/container stacks path, no hardcoded DB password, no Postgres port publish | ✓ VERIFIED | Read directly |
| `Dockerfile` | Pinned checksum-verified restic + Docker CLI/Compose | ✓ VERIFIED | Read directly |
| `server/src/lib/stacks-dir.ts` | `assertStacksDirMatchesHost()`, path-traversal guard | ✓ VERIFIED | Unit-tested, independently re-run |
| `server/src/application/stack-service.ts` | Broadcasts on every transition, env-write config_changed | ✓ VERIFIED | Unit-tested |
| `server/src/application/backup-service.ts` | Repo-config guard, broadcasts, RESTORE transition guard, WR-05 fail-fast | ✓ VERIFIED | 76 unit tests independently re-run, passing |
| `server/prisma/schema/stack.prisma` | `configError`, `lastEnvHash` fields | ✓ VERIFIED | Read directly |
| `server/src/jobs/file-watcher.ts` | `.env` watching, configError set/clear | ✓ VERIFIED | 42 unit tests (part of 71 independently re-run) |
| `client/src/components/domain/stack/stack-list.tsx` | Red config-error pill | ✓ VERIFIED | Read directly, wired to `stack.configError` |
| `client/src/routes/app/stacks/[id].tsx` | Destructive Alert for configError, refetch-on-save | ✓ VERIFIED | Read directly; unit test independently re-run (4/4 pass in isolation) |
| `server/src/routes/imports.ts` | `requireAuth`-gated import routes | ✓ VERIFIED | Read directly, `requireAuth` is first `onRequest` hook |
| `docs/deployment.md`, `README.md` | Deployment quickstart, env-var reference | ✓ VERIFIED | Both exist; `POSTGRES_PASSWORD` requirement documented |
| `.env.example` | Complete, truthful env template | ⚠️ UNVERIFIED BY ME | Filesystem permission denies read access to `.env*` files in this session (independently reproduced) — same restriction plan sessions reported. Cannot be graded directly; the deployment path it feeds was live-verified end-to-end in 05.1-08. |

### Key Link Verification

| From | To | Via | Status |
|------|-----|-----|--------|
| `StackService.transitionStatus`/`publishConfigChanged` | `/api/events` SSE | `StateBroadcaster.publish` | ✓ WIRED (unit-tested call sites) |
| `BackupService.writeStackStatus` | `/api/events` SSE | `StateBroadcaster.publish` | ✓ WIRED (unit-tested call sites) |
| `FileWatcher.handleFileChange`/`handleEnvChange` | `Stack.configError`/`configChanged` | Repository writes | ✓ WIRED (unit-tested + grep-confirmed in repository) |
| `config_error` SSE event | UI (list pill, detail Alert, toast) | `use-stacks.ts`/`use-stack.ts`/`stack-list.tsx`/`[id].tsx` | ✓ WIRED (grep-confirmed, hook unit tests independently re-run) |
| `docker-compose.yml` stacks volume (host) | `DOCKTOR_STACKS_DIR` (container) | `DOCKTOR_STACKS_HOST_DIR` | ✓ WIRED (read directly, identical both sides) |
| `server/src/index.ts` | `syncDatabaseSchema()` → `app.listen()` | Startup sequence | ✓ WIRED (read directly; ordering confirmed: stacks-dir assertion → schema-sync → buildApp → listen) |
| `client/src/routes/app/stacks/index.tsx` | `/stacks/import` | "Import Existing Stack" action | ✓ WIRED (documented + e2e spec exists) |

### Behavioral Spot-Checks (performed by this verifier, not carried from SUMMARY)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Server workspace type-checks clean | `yarn workspace @docktor/server tsc --noEmit` | No output, exit 0 | ✓ PASS |
| Client workspace type-checks clean | `yarn workspace @docktor/client tsc --noEmit` | No output, exit 0 | ✓ PASS |
| Server unit+integration suite | `yarn workspace @docktor/server test --run` | 499 unit tests passed; 4 integration files fail with P1001 (environmental) | ✓ PASS (unit) / ✗ ENV-BLOCKED (integration) |
| `stack-status-machine.test.ts` (CR-01 fix regression coverage) | `yarn workspace @docktor/server test test/unit/domain/stack-status-machine.test.ts --run` | 16/16 pass | ✓ PASS |
| `backup-service.test.ts` | `yarn workspace @docktor/server test test/unit/application/backup-service.test.ts --run` | 76/76 pass | ✓ PASS |
| `stacks-dir.test.ts` + `schema-sync.test.ts` + `file-watcher.test.ts` + `disk-checker.test.ts` | combined run | 71/71 pass | ✓ PASS |
| Client full unit suite (parallel) | `yarn workspace @docktor/client test --run` | 121/137 pass, 13 timeouts (5-16s) — matches documented CPU-contention flake pattern | ⚠️ FLAKY (see isolation re-run) |
| `stack-detail-page.test.tsx` in isolation (was in the flaky batch) | isolated run | 4/4 pass | ✓ PASS |
| `service-upgrade-dialog.test.tsx` in isolation (was in the flaky batch) | isolated run | 9/9 pass | ✓ PASS |
| `.env.example` readable by this verifier | Read tool + Bash `grep` | Both denied by filesystem permission settings | ℹ️ CONFIRMS documented restriction |
| No uncommitted source changes | `git status --short` | Only `.planning/` metadata + untracked `.gsd/` | ✓ PASS |
| No debt markers in phase's modified files | `grep -nE "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across 14 key files | Zero matches | ✓ PASS |
| All 6 REVIEW-FIX commits exist | `git log` / `git show --stat` per commit | All 6 present with matching messages | ✓ PASS |

### Anti-Patterns Found

None in the phase's modified source files (checked: schema-sync.ts, stacks-dir.ts, stack-service.ts, backup-service.ts, file-watcher.ts, disk-checker.ts, imports.ts, stack-repository.ts, `[id].tsx`, stack-list.tsx, brownfield-import.tsx, import.tsx, docker-compose.yml, Dockerfile).

### Requirements Coverage

n/a — confirmed no-op (see Requirements Traceability above).

### Deferred Items

None — all 10 scoped items (B1, B2, B3, M1-M6, N1) have corresponding plans and code-level evidence; nothing was pushed to a later phase.

## Gaps Summary

No BLOCKER-level gaps. Every one of the 10 scoped defects (3 blockers, 6 majors, 1 minor) has a corresponding code change, present in the current working tree, independently re-confirmed by this verifier through direct code reads and re-run tests — including the 1 Critical + 5 Warning findings from the phase's own code-review pass, all fixed and committed.

What remains is a consistent, well-documented set of **environmental** and **live-instance** verification gaps, all pre-existing in WINDOWS.md (9 open items) and none newly discovered by this verification:

1. **Server integration test suite has never been observed to exit 0** in any session (this phase's own or this verification's) — a confirmed host-level TCP-payload-to-Docker-published-port restriction, reproduced independently here for the third time. This is the phase's own primary deliverable (B1) partially unproven: the client e2e half is proven, the server integration half is not.
2. **Several live UI/UX confirmations require a running instance and a browser**, unavailable in every session to date (two-tab SSE live confirmation, .env/compose-error live UI confirmation).
3. **One live DooD re-verification is outstanding** for safety reasons (the only prior attempt caused a real incident on a shared host) rather than a code concern.
4. **`.env.example` could not be read by this verifier** (same permission restriction reported by 3 plan sessions) — the code that consumes it, and the deployment path it feeds, were both independently verified by other means.

These are all legitimate `human_needed` items per the task's explicit guidance, not gaps — the underlying code changes are unit-tested, reviewed, and (for the highest-risk item, B2's schema-sync) were live-verified end-to-end with concrete evidence in a prior session. Recommend a human (or an unrestricted CI runner) close out WINDOWS.md's 9 open items before treating Phase 05.1 as fully closed, but nothing here should block proceeding to Phase 6 on code-readiness grounds.

---

*Verified: 2026-09-02T13:57:30Z*
*Verifier: Claude (gsd-verifier)*
