---
status: complete
phase: 05.1-stabilization-fix-blockers-and-majors-surfaced-during-testin
source: [05.1-VERIFICATION.md]
started: 2026-09-02T15:45:00Z
updated: 2026-09-03T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Run `yarn workspace @docktor/server test:integration` on a host without the sandbox's TCP-payload-to-Docker-published-port restriction
expected: Exits 0 across all 4 integration test files (setup-concurrency, setup-wizard-flow, stacks, imports)
result: issue
reported: "all test failed due to the following error: Error: startContainer(): `prisma db push` failed while applying the schema to the test database. However, they were successful in github actions. [Correction: only 4 failed, not all; user suspects this may be a local setup/environment issue rather than a code defect]"
severity: blocker

### 2. With two browser tabs open on the same stack, press Deploy/Stop/Restart/Update in one tab and confirm the status badge updates live in both tabs; save compose/env in one tab and confirm the config-changed banner appears live in both
expected: Both tabs reflect the SSE-broadcast status/config-changed state without a manual reload
result: pass

### 3. Edit a running stack's .env file directly on disk and confirm the config-changed badge appears within the watcher's detection window; introduce a YAML syntax error into a running stack's compose file and confirm a red config-error indicator appears on both the stack list and detail page without a manual reload, then confirm it clears when the file is fixed
expected: Both indicators appear/clear live, driven by FileWatcher + SSE + the new UI badges
result: issue
reported: "it seems there is an issue with the volumes. When running the docker-compose, I'll get the following error: docktor | Error: Stacks directory path mismatch: DOCKTOR_STACKS_HOST_DIR (\"/mnt/c/Users/D070307/workspace/docktor/dev-data/docktor/stacks\") does not match the container-side DOCKTOR_STACKS_DIR (\"/opt/docktor/stacks\"). Docktor runs Docker-outside-of-Docker — the stacks directory must be mounted at the identical absolute path on both the host and inside this container... at assertStacksDirMatchesHost (file:///app/dist/server/lib/stacks-dir.js:52:15)"
severity: blocker
note: "Reproduction of already-tracked gap G-05.1-2 (still unresolved) — server refuses to boot, so Test 3's actual FileWatcher/SSE behavior could not be exercised."

### 4. Create a stack with a relative bind-mount volume (e.g. `./volumes/data:/var/opt/app`) through Docktor's own deploy pipeline, in an isolated Docker host with no unrelated running projects, and confirm the data lands at the identical host path
expected: Host-side directory appears under the stack's own directory, matching the container-side write
result: issue
reported: "couldnt test due to same error as before. this is a blocker"
severity: blocker
note: "Blocked by the same still-open G-05.1-2 root cause as Test 3 — server won't boot with a non-default DOCKTOR_STACKS_HOST_DIR, so the relative bind-mount deploy path could not be exercised."

### 5. Run an actual restic backup, then a restore, against a real deployed stack, and confirm the restored files match what the containers wrote
expected: Backup archive and restored files are byte-identical to the live stack directory
result: issue
reported: "same issue, blocked as well"
severity: blocker
note: "Blocked by the same still-open G-05.1-2 root cause as Tests 3 and 4 — no deployed stack available to back up or restore."

### 6. Run `server/test/integration/imports.test.ts` on an unrestricted host and confirm all 4 unauthenticated-request tests return 401, the scan+adopt round trip succeeds, and the T-05-09 410 regression guard still holds
expected: All assertions pass against a real Postgres-backed Fastify instance
result: issue
reported: "failes: Error: startContainer(): `prisma db push` failed while applying the schema to the test database.\n--- stdout ---\n\n--- stderr ---\n\n at startContainer test/integration/setup.ts:46:15 ... at test/integration/imports.test.ts:20:9\nCaused by: Error: spawnSync C:\\Users\\D070307\\workspace\\docktor\\node_modules\\.bin\\prisma ENOENT"
severity: blocker
note: "Same failure category as G-05.1-1 (test 1), now with a concrete cause: spawnSync ENOENT on node_modules\\.bin\\prisma — the prisma CLI binary is not resolvable at that path on this Windows host. Folded into G-05.1-1 rather than opening a duplicate gap."

### 7. Native Windows integration-suite run (G-05.1-1 final confirmation)
expected: |
  Run `yarn workspace @docktor/server test:integration` on a native Windows host
  (not WSL) after a normal `yarn install`. No `spawnSync ... ENOENT` appears; any
  remaining failure is environmental (DB reachability), never ENOENT.
result: issue
reported: "the ENOENT error did not occur, but another windows related issue: FAIL test/integration test/integration/imports.test.ts > Imports API (post-setup brownfield scan/adopt/migrate) > POST /api/stacks/import/scan → returns a ScanResult-shaped body for an authenticated request. AssertionError: expected 'C:/Users/D070307/AppData/Local/Temp/d…' to be 'C:\\Users\\D070307\\AppData\\Local\\Temp\\d…' // Object.is equality. Expected: \"C:\\Users\\D070307\\AppData\\Local\\Temp\\docktor-imports-test-6oKgpG\", Received: \"C:/Users/D070307/AppData/Local/Temp/docktor-imports-test-6oKgpG\". at test/integration/imports.test.ts:108:42 (expect(body.stacks[0].directory).toBe(scanDir)). Test Files: 1 failed | 3 passed (4). Tests: 1 failed | 18 passed (19)."
severity: major
note: "G-05.1-1's ENOENT is confirmed fixed on native Windows. This is a new, previously-untracked defect: the scan API returns a forward-slash-normalized directory path while the test's raw Windows path (and presumably the on-disk path passed in) uses backslashes, so a Windows client comparing the returned path against its own OS-native path representation sees a mismatch."

### 8. Docker Desktop host confirmation (G-05.1-3 final confirmation)
expected: |
  On the Windows/Docker Desktop host that originally reported G-05.1-3, run
  `docker compose up -d` with default settings and confirm
  `docker exec docktor ls -la /opt/docktor/stacks` now succeeds.
result: pass

### 9. Apply the two blocked .env.example / .env.production header edits (G-05.1-2 Task 3)
expected: |
  A developer with .env* file access changes `.env.local` to `.env` in
  .env.example line 3 and .env.production line 12, then confirms with:
  `! git grep -nE 'env\.local' -- . ':!.planning' ':!.gitignore' && echo VERIFIED`
result: pass

### 10. Native Windows re-run of imports.test.ts:108 (G-05.1-4 final confirmation)
expected: |
  On the native Windows host that originally reported G-05.1-4, re-run
  `yarn workspace @docktor/server test:integration` and confirm
  imports.test.ts:108's assertion (body.stacks[0].directory === scanDir)
  now passes with no edit to the test itself.
result: pass

## Summary

total: 10
passed: 4
issues: 6
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-05.1-4
  truth: "Run `yarn workspace @docktor/server test:integration` on a native Windows host (not WSL) after a normal `yarn install`. No `spawnSync ... ENOENT` appears; any remaining failure is environmental (DB reachability), never ENOENT."
  status: resolved
  resolution: "Closed by 05.1-12 (commits fc8ef62, ed41c85, f51f741, 36c2085). Added path.normalize() at the single point brownfield-scanner.ts's scan() consumes fast-glob's absolute-path output, restoring OS-native separators for both the path and directory fields in one place; added client-side directoryDisplayName() (client/src/lib/path-display.ts) tolerant of either separator so brownfield-import.tsx and migration-wizard.tsx don't regress on the now-native paths. Regression proven on this Linux host via a path.win32-mocked unit test (brownfield-scanner-windows-paths.test.ts) plus a POSIX no-op pinning test; full server unit suite green (33 files/512 tests), tsc clean. Outstanding: a human on the original native-Windows host still needs to re-run yarn workspace @docktor/server test:integration and confirm imports.test.ts:108 now passes (recorded in 05.1-12-SUMMARY.md)."
  reason: "User reported: the ENOENT error did not occur, but another windows related issue: FAIL test/integration test/integration/imports.test.ts > Imports API (post-setup brownfield scan/adopt/migrate) > POST /api/stacks/import/scan → returns a ScanResult-shaped body for an authenticated request. AssertionError: expected 'C:/Users/D070307/AppData/Local/Temp/d…' to be 'C:\\Users\\D070307\\AppData\\Local\\Temp\\d…' // Object.is equality. Expected: \"C:\\Users\\D070307\\AppData\\Local\\Temp\\docktor-imports-test-6oKgpG\", Received: \"C:/Users/D070307/AppData/Local/Temp/docktor-imports-test-6oKgpG\". at test/integration/imports.test.ts:108:42."
  severity: major
  test: 7
  root_cause: "server/src/infrastructure/brownfield-scanner.ts's scan() method builds DiscoveredStack.path/directory from fast-glob's absolute-path output (fg(..., { absolute: true })) without ever renormalizing separators. Fast-glob's EntryTransformer unconditionally unixifies ALL absolute paths (replaces \\ with /) on every platform, including Windows — documented upstream behavior. Node's native path module resolves to path.win32 on Windows, and path.win32.dirname() does not restore native separators on a string that already contains only forward slashes; it just slices at the last separator found. Neither brownfield-scanner.ts (path: filePath at line 103, directory: path.dirname(filePath) at line 104) nor server/src/routes/imports.ts's scan handler (returns the ScanResult verbatim) ever normalizes the path back to OS-native form, so on Windows both fields stay forward-slash while a Windows client's own path representation (e.g. scanDir from fs.mkdtemp) is backslash-separated. Confirmed via direct source read and a path.win32 repro; distinct mechanism from G-05.1-1 (Prisma CLI resolution) — no shared code path."
  artifacts:
    - path: server/src/infrastructure/brownfield-scanner.ts
      issue: "Lines 70-76 call fg(..., { absolute: true }), whose output is always POSIX-unixified by fast-glob even on Windows; lines 102-104 derive path/directory directly from that output without calling path.normalize(), so both fields stay forward-slash-separated on native Windows"
    - path: server/src/routes/imports.ts
      issue: "Scan route (lines 19-31) returns the ScanResult verbatim with no path post-processing — confirmed pass-through, not an independent source of the bug"
  missing:
    - "Apply path.normalize() (resolves to path.win32.normalize on Windows) to filePath and path.dirname(filePath) in brownfield-scanner.ts's scan() before pushing onto the results, restoring OS-native separators for both the path and directory fields — normalize once, immediately after the fg() call, to cover both in one place"
  debug_session: .planning/debug/windows-scan-path-separator.md

- gap_id: G-05.1-1
  truth: "Exits 0 across all 4 integration test files (setup-concurrency, setup-wizard-flow, stacks, imports)"
  status: resolved
  resolution: "Closed by 05.1-09 (commits c26bd20, 30c877e, 6926d26). Added server/src/lib/prisma-cli.ts's resolvePrismaCliEntrypoint(), resolving the Prisma CLI's JS entrypoint via Node module resolution (createRequire(import.meta.url)) instead of a hardcoded POSIX .bin/prisma path; both call sites (setup.ts's startContainer(), schema-sync.ts's defaultRunCli) now launch it via process.execPath with an argv array and no shell. 4 new unit tests pass, full server unit suite green (32 files/507 tests), tsc clean. Local run got past CLI resolution and failed only on the separately-documented Docker-daemon environmental block. Outstanding: a human on the original native-Windows host still needs to confirm the ENOENT no longer occurs (recorded in 05.1-09-SUMMARY.md)."
  reason: "User reported: all test failed due to the following error: Error: startContainer(): `prisma db push` failed while applying the schema to the test database. However, they were successful in github actions. Correction: only 4 (individual) tests failed, not all files; user suspects this may be a local setup/environment issue rather than a code defect. Reconfirmed under Test 6 (imports.test.ts specifically) with a concrete cause this time: `Caused by: Error: spawnSync C:\\Users\\D070307\\workspace\\docktor\\node_modules\\.bin\\prisma ENOENT` — the prisma CLI binary is not resolvable at that path on this Windows host, which is what makes `prisma db push` fail inside startContainer()."
  severity: blocker
  test: 1
  root_cause: "server/test/integration/setup.ts:14 hardcodes the Prisma CLI path as a POSIX-shaped, extensionless `.bin/prisma` path and invokes it via execFileSync (setup.ts:38) with no shell and no platform branching. On Linux/macOS (incl. CI's ubuntu-latest) Yarn's node-modules linker creates `.bin/prisma` as a symlink to a shebang'd JS file, which POSIX exec() runs transparently. On native Windows the same linker convention instead produces a `.cmd`/`.ps1` shim, not a directly-executable file at the bare extensionless path — CreateProcess cannot resolve it, producing the observed ENOENT. Genuine cross-platform code defect; CI never catches it because it only runs on ubuntu-latest."
  artifacts:
    - path: server/test/integration/setup.ts
      issue: "Line 14 hardcodes an extensionless POSIX-shaped `.bin/prisma` path; line 38 execFileSync's it with no shell/platform handling, so it ENOENTs on native Windows"
  missing:
    - "Resolve Prisma's JS entrypoint via Node module resolution (e.g. require.resolve('prisma/build/index.js')) and invoke it via execFileSync(process.execPath, [entrypoint, ...]) instead of the OS-shaped .bin shim path — works identically cross-platform, no shell, preserves the argv-array form T-05.1-02 required (avoid re-adding shell:true, which would reopen the shell-injection risk T-05.1-02 closed)"
  debug_session: .planning/debug/server-test-integration-prisma-enoent.md

- gap_id: G-05.1-2
  truth: "Following docs/deployment.md exactly (cp .env.example .env.local; set DOCKTOR_STACKS_HOST_DIR in .env.local; docker compose up -d) boots the server successfully with the custom stacks directory."
  status: resolved
  resolution: "Closed by 05.1-10 (commits 93adfbb, 4276f3c). docker-compose.yml's env_file: entries renamed .env.local -> .env so Compose's own top-level ${VAR} interpolation (which only ever reads a project-root .env, never a service-level env_file:) sees the operator's DOCKTOR_STACKS_HOST_DIR; verified live with `docker compose config` that a custom value now propagates to DOCKTOR_STACKS_DIR and both sides of the stacks bind mount. Added root .dockerignore excluding deployment env files from the build context (T-05.1-46). docs/deployment.md and README.md rewritten for the new filename with an upgrade-path troubleshooting row. Task 3 (matching header-comment fix in .env.example/.env.production) remains blocked: workspace permission settings deny Read/Write on .env* paths for both the executor and the orchestrator. Two one-line edits are outstanding — see 05.1-10-SUMMARY.md's 'Blocked: Task 3' section — and require a developer with .env* access to apply."
  reason: "User reported the server refuses to boot with 'Stacks directory path mismatch: DOCKTOR_STACKS_HOST_DIR (...) does not match the container-side DOCKTOR_STACKS_DIR (\"/opt/docktor/stacks\")' after setting DOCKTOR_STACKS_HOST_DIR in .env.local — reproduced twice, once with a Windows path and once with the corrected /mnt/c/... POSIX path, same failure both times. Confirmed root cause via `docker compose config`: docker-compose.yml's `environment: DOCKTOR_STACKS_DIR: ${DOCKTOR_STACKS_HOST_DIR:-/opt/docktor/stacks}` and the matching volume line are resolved by Compose's own variable interpolation, which only reads a file literally named `.env` (or `--env-file`/shell env) — NOT `.env.local`. `env_file: .env.local` only injects vars into the container's runtime environment, so the app process sees the real custom DOCKTOR_STACKS_HOST_DIR while Compose-time interpolation silently fell back to the /opt/docktor/stacks default for DOCKTOR_STACKS_DIR and the volume host side. This affects ANY non-default DOCKTOR_STACKS_HOST_DIR set per docs/deployment.md, not just Windows paths — not a UAT test-list item, a defect discovered during Test 3 while investigating an unrelated report."
  severity: blocker
  test: 3
  root_cause: "docker-compose.yml uses top-level ${DOCKTOR_STACKS_HOST_DIR:-/opt/docktor/stacks} Compose-variable interpolation (resolved at compose-parse time, not container-runtime) in two places: the DOCKTOR_STACKS_DIR environment entry (line 30) and both sides of the stacks volumes bind mount (line 50). Compose's top-level ${VAR} interpolation only reads the real process/shell env or a file literally named .env (or --env-file) — never env_file: service directives, and never .env.local. docs/deployment.md's Quickstart (cp .env.example .env.local; edit it; plain docker compose up -d, no --env-file) means Compose's interpolation never sees the custom value and silently falls back to /opt/docktor/stacks for both the environment entry and the volume, while env_file: .env.local correctly injects the real custom value into the container's runtime env — producing the mismatch assertStacksDirMatchesHost() correctly detects and fails fast on. Live-reproduced via `docker compose config`: without --env-file, all three (DOCKTOR_STACKS_DIR, volume source, volume target) render as the default, ignoring .env.local; with --env-file .env.local they resolve correctly. Same Compose interpolation-vs-env_file class of bug already found and fixed once this phase for POSTGRES_PASSWORD (WR-04)."
  artifacts:
    - path: docker-compose.yml
      issue: "${DOCKTOR_STACKS_HOST_DIR:-...} interpolation (line 30 environment entry, line 50 volume) reads Compose's .env, not .env.local, so a value set only in .env.local per docs/deployment.md never reaches DOCKTOR_STACKS_DIR or the volume host side"
    - path: docs/deployment.md
      issue: "Quickstart instructs `cp .env.example .env.local` then plain `docker compose up -d`, with no --env-file flag — the documented path itself triggers the mismatch for any non-default DOCKTOR_STACKS_HOST_DIR"
  missing:
    - "Rename the deployment env file from .env.local to .env everywhere referenced (docker-compose.yml's env_file: entries, docs/deployment.md, README.md, .env.example's header) — Compose auto-discovers a project-root .env for interpolation with zero extra flags, and .env is already gitignored so there is no secret-exposure regression. (Restructuring the volume mapping to avoid interpolation is not viable — env_file: cannot populate a volumes: entry.)"
  debug_session: .planning/debug/stacks-host-dir-env-local-mismatch.md

- gap_id: G-05.1-3
  truth: "The managed stacks directory (DOCKTOR_STACKS_DIR, default /opt/docktor/stacks) exists inside the docktor container after a normal `docker compose up -d` with default settings."
  status: resolved
  resolution: "Closed by 05.1-11 (commits 49d8703, fcda20e). Added ensureStacksDir() to server/src/lib/stacks-dir.ts — recursive, idempotent mkdir on the resolved stacks path with a path-naming, cause-preserving error on failure — wired into server/src/index.ts's boot sequence immediately after assertStacksDirMatchesHost() and before schema-sync/buildApp()/listen(). 4 new unit tests (real mkdtemp filesystem) plus a live tsx boot proof confirm the directory now exists after boot. Deferred, more-severe follow-up (mkdir cannot prove the path is genuinely the host-backed bind mount vs. the container's own writable layer) recorded as .planning/todos/pending/2026-09-03-stacks-dir-mount-point-not-verified.md. Outstanding: a human on the original Windows/Docker Desktop host should confirm `docker exec docktor ls -la /opt/docktor/stacks` now succeeds (recorded in 05.1-11-SUMMARY.md)."
  reason: "User reported (Windows host, Docker Desktop, default DOCKTOR_STACKS_HOST_DIR, server booted successfully with no mismatch error): /opt/docktor/stacks does not exist inside the running container. Verified in server/src: nothing at startup (server/src/index.ts calls assertStacksDirMatchesHost() but never creates the directory) or in server/src/lib/stacks-dir.ts ever calls fs.mkdir on getStacksDir() itself — only per-stack subdirectories are created on demand (server/src/infrastructure/stack-filesystem.ts:11). The app relies entirely on Docker's bind-mount auto-vivification of the host-side directory (confirmed this works on native Linux dockerd in a throwaway repro), but that assumption apparently does not hold on the user's Windows/Docker Desktop setup, leaving the app with no top-level stacks directory and no defensive fallback."
  severity: major
  test: 3
  root_cause: "The server never creates its own managed-stacks directory. server/src/index.ts's startup sequence (assertStacksDirMatchesHost() -> syncDatabaseSchema() -> buildApp() -> listen()) contains no fs.mkdir call; stacks-dir.ts's getStacksDir()/assertStacksDirMatchesHost() are pure path-string helpers with no filesystem side effect; the app's only mkdir (stack-filesystem.ts createDirectory()) is scoped to per-stack subdirectories, on-demand, never the top-level dir, and nothing at boot invokes it. This means the app depends entirely on Docker auto-creating the missing bind-mount host directory. This reproduces even with byte-identical, mismatch-free default settings (DOCKTOR_STACKS_HOST_DIR == DOCKTOR_STACKS_DIR == /opt/docktor/stacks) — independent of G-05.1-2. Docker's short-syntax bind-mount auto-creation is legacy/best-effort, not a guaranteed contract, and Docker Desktop's WSL2 integration handles a bare Linux absolute path (no real Windows-drive/WSL-distro meaning) via a documented, different code path than native Linux dockerd's direct mkdir -p — consistent with native Linux working and Windows/Docker Desktop failing."
  artifacts:
    - path: server/src/index.ts
      issue: "Calls assertStacksDirMatchesHost() but never ensures the stacks directory itself exists"
    - path: server/src/lib/stacks-dir.ts
      issue: "getStacksDir() / assertStacksDirMatchesHost() never fs.mkdir the top-level stacks directory — only stack-filesystem.ts creates per-stack subdirectories on demand"
  missing:
    - "Add await fs.mkdir(getStacksDir(), { recursive: true }) early in server/src/index.ts's startup sequence, after assertStacksDirMatchesHost() passes and before buildApp()/listen(), consistent with the file's existing fail-fast-before-anything-else-starts ordering — so the app does not depend solely on the Docker bind-mount auto-creating the host-side directory"
  debug_session: .planning/debug/stacks-dir-not-created-on-boot.md
