---
phase: 01-mvp-completion
plan: 04
subsystem: validation
tags: [zod, settings, cmdk, shadcn, typescript, timezone, iana]

# Dependency graph
requires:
  - phase: 01-mvp-completion-01-02
    provides: shared validation foundation (Zod installed, settings.ts base schema)

provides:
  - generalSettingsSchema with IANA timezone validation and URL validation
  - generalSettingsUpdateSchema as partial (all fields optional)
  - GeneralSettings and GeneralSettingsUpdate TypeScript types
  - client/src/components/ui/command.tsx shadcn Command component (cmdk wrapper)
  - client/src/components/ui/dialog.tsx shadcn Dialog component (cmdk dependency)

affects: [01-07, settings-ui, timezone-combobox]

# Tech tracking
tech-stack:
  added: [cmdk ^1.1.1]
  patterns:
    - IANA timezone validation via Intl.supportedValuesOf with Intl.DateTimeFormat fallback
    - generalSettingsSchema.partial() for partial-update endpoint schemas

key-files:
  created:
    - client/src/components/ui/command.tsx
    - client/src/components/ui/dialog.tsx
  modified:
    - shared/src/validation/settings.ts

key-decisions:
  - "IANA timezone validation uses Intl.supportedValuesOf + Intl.DateTimeFormat fallback because some Node.js environments (limited ICU data) omit UTC from supportedValuesOf but DateTimeFormat still accepts it"

patterns-established:
  - "Timezone validation pattern: try Intl.supportedValuesOf first, fall back to Intl.DateTimeFormat constructor to handle edge cases like UTC"

requirements-completed: [SET-03]

# Metrics
duration: 6min
completed: 2026-03-11
---

# Phase 1 Plan 4: Settings Validation Schemas and shadcn Command Component Summary

**Zod generalSettingsSchema with IANA timezone + URL validation, plus cmdk-backed shadcn Command component for timezone combobox**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-11T14:21:27Z
- **Completed:** 2026-03-11T14:27:41Z
- **Tasks:** 2
- **Files modified:** 3 (+ 2 created)

## Accomplishments

- Implemented `generalSettingsSchema` with `instanceName` (required string), `baseUrl` (URL or empty string), and `timezone` (IANA validation)
- Implemented `generalSettingsUpdateSchema` as partial schema for PUT /api/settings/general endpoints
- Installed cmdk ^1.1.1 and generated `command.tsx` via shadcn CLI — exports Command, CommandInput, CommandList, CommandEmpty, CommandGroup, CommandItem
- All 5 settings-phase1 tests GREEN; shared build and client build both pass

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend shared settings validation schemas** - `a07b7fa` (feat)
2. **Task 2: Install cmdk + generate shadcn Command component** - `9369a97` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `shared/src/validation/settings.ts` - Added generalSettingsSchema, generalSettingsUpdateSchema, GeneralSettings type, GeneralSettingsUpdate type, isValidIANATimezone helper
- `client/src/components/ui/command.tsx` - Generated by shadcn CLI; wraps cmdk primitives with shadcn new-york styling
- `client/src/components/ui/dialog.tsx` - Generated by shadcn CLI as a dependency of Command

## Decisions Made

- Used `Intl.DateTimeFormat` constructor fallback in addition to `Intl.supportedValuesOf("timeZone")` for IANA validation. In this Node.js environment UTC is not returned by `supportedValuesOf` due to limited ICU data, but `Intl.DateTimeFormat` correctly accepts it.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added Intl.DateTimeFormat fallback for UTC timezone validation**
- **Found during:** Task 1 (TDD GREEN phase — test run after schema implementation)
- **Issue:** `Intl.supportedValuesOf("timeZone")` in this Node.js environment returns no "UTC" entry, causing `generalSettingsSchema.safeParse({ timezone: "UTC" })` to fail the IANA refine check
- **Fix:** Extracted `isValidIANATimezone()` helper that first checks `supportedValuesOf` then tries `new Intl.DateTimeFormat(undefined, { timeZone: tz })` — a try/catch confirming the runtime accepts the value
- **Files modified:** shared/src/validation/settings.ts
- **Verification:** All 5 settings-phase1 tests GREEN including "accepts valid IANA timezones: UTC"
- **Committed in:** a07b7fa (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — Bug)
**Impact on plan:** Required for correctness — UTC is a valid IANA timezone and the test spec requires it to pass. No scope creep.

## Issues Encountered

None beyond the UTC timezone validation fix above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `generalSettingsSchema` and `generalSettingsUpdateSchema` are ready for Plan 05 (server-side settings endpoints) and Plan 07 (Settings UI)
- `command.tsx` is ready for timezone Combobox in Plan 07
- No blockers

---
*Phase: 01-mvp-completion*
*Completed: 2026-03-11*
