---
phase: 04-backup-restore
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - server/prisma/schema/backup.prisma
autonomous: true
gap_closure: true
requirements: [BCK-02, BCK-04]

must_haves:
  truths:
    - "Backup detail page displays log lines for IN_PROGRESS backups"
    - "Backup detail page displays stored log lines for COMPLETED backups"
    - "Backup detail page displays stored log lines for FAILED backups"
  artifacts:
    - path: "server/prisma/schema/backup.prisma"
      provides: "logLines String[] column in Backup model"
      contains: "logLines"
  key_links:
    - from: "Prisma schema"
      to: "PostgreSQL database"
      via: "prisma db push"
      pattern: "ALTER TABLE.*logLines"
---

<objective>
Sync the Prisma schema with the PostgreSQL database to add the missing `logLines` column.

Purpose: The Prisma schema was updated to include `logLines String[]` on the Backup model, but `prisma db push` was never run. This causes all backup log queries to return undefined/null, resulting in "No output yet..." being displayed on the backup detail page regardless of backup status.

Output: Database column exists and log lines are persisted and displayed correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backup-restore/04-UAT.md (Gaps 5, 6, 7)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync Prisma schema with database</name>
  <files>server/prisma/schema/backup.prisma</files>
  <action>
Run `yarn workspace @docktor/server prisma db push` to sync the Prisma schema with the PostgreSQL database.

This will add the `logLines` column to the `Backup` table in PostgreSQL. The column already exists in the Prisma schema (`logLines String[]` on line 11 of backup.prisma), but was never pushed to the database.

After running the command:
1. Verify the command completes without errors
2. Check the database has the new column by inspecting Prisma's output
3. Run `yarn workspace @docktor/server prisma generate` to ensure the Prisma client is up to date (if not already done by db push)
  </action>
  <verify>
    <automated>cd C:/Users/D070307/workspace/docktor && yarn workspace @docktor/server prisma db push 2>&1 | grep -E "(logLines|Your database is now in sync)"</automated>
  </verify>
  <done>PostgreSQL database has logLines column in Backup table; Prisma client is generated</done>
</task>

<task type="auto">
  <name>Task 2: Verify log lines are persisted and returned</name>
  <files>-</files>
  <action>
Verify the fix works end-to-end:

1. Start the server if not running: `yarn workspace @docktor/server dev`
2. Trigger a manual backup for any stack via the UI or API
3. Check the backup detail page shows streaming log lines (not "No output yet...")
4. After backup completes, verify log lines are still visible
5. Check the database directly to confirm logLines array is populated:
   ```sql
   SELECT id, status, "logLines" FROM "Backup" ORDER BY "createdAt" DESC LIMIT 1;
   ```

The logLines column should now contain an array of strings from the restic output.
  </action>
  <verify>
    <automated>cd C:/Users/D070307/workspace/docktor && yarn workspace @docktor/server prisma db execute --stdin <<< "SELECT id, status, array_length(\"logLines\", 1) as line_count FROM \"Backup\" WHERE array_length(\"logLines\", 1) > 0 LIMIT 1;" 2>&1 || echo "No backups with logs yet - will verify manually"</automated>
  </verify>
  <done>Log lines are persisted to database and displayed on backup detail page</done>
</task>

</tasks>

<verification>
1. `prisma db push` completes successfully
2. Backup detail page shows log output instead of "No output yet..."
3. Existing IN_PROGRESS, COMPLETED, and FAILED backups display their logs (after new backups are created)
</verification>

<success_criteria>
- UAT Test 10 (Stream Live Backup Logs): PASS
- UAT Test 11 (View Completed Backup Logs): PASS
- UAT Test 12 (View Failed Backup Error): PASS - logs visible alongside error alert
</success_criteria>

<output>
After completion, create `.planning/phases/04-backup-restore/04-07-SUMMARY.md`
</output>
